本故事纯属虚构，如有雷同，纯属巧合！

**故事背景**

小白和小胖是同事入职公司的，每次绩效考核总难免碰上两个人的竞争，小胖的人缘比较好，小白更偏向与跟机器打交道。快到年底了，他们的直属 leader 小 L 犯难了，组内只有一个优秀名额，可是小白和小胖这一年表现都比较亮眼，在小 L 眼中，小胖善于和人打好交道，很多次和业务端一起搞定了很多难搞的项目；小白则喜欢钻研技术，是技术攻关的一把尖刀，但名额只有一个，这可把他难住了。

恰巧本月有一个新星大pk，小 L 让他们两个都参加， 看谁的表现更好。新星大 pk 是新员工都可以参加的比赛，比赛中他们随机分到任务，然后进行pk。小胖通过内部认识的人知道了任务分为普通任务和死亡任务，普通任务就是难度中等的题目，死亡任务就是高难度的题目。普通任务和死亡任务随机抽取，小胖私下咨询了好几个参加过的同事，发现任务的分发是一个业务的同事，通过和这位同事的私下交流，小胖信心满满去参加比赛。

赛场上，小胖"运气好"地抽到了一个普通任务，任务如下：

![img](image/0f391fcb6d664bf9b882b7a5e631a571.jfif)

8 道对错题目，对 6 道及以上晋级。

```java
	public static void main(String[] args) {
		Integer num1=56;
		Integer num2=56;
		System.out.println(num1==num2);
		System.out.println(num1.equals(num2));
		
		Integer num3=34567;
		Integer num4=34567;
		System.out.println(num3==num4);
		System.out.println(num3.equals(num4));
		
		Integer num21=-56;
		Integer num22=-56;		
		System.out.println(num21==num22);
		System.out.println(num21.equals(num22));
		
		Integer num23=-34567;
		Integer num24=-34567;
		System.out.println(num23==num24);		
		System.out.println(num23.equals(num24));
	}
```

小白很不幸的抽到了一个死亡任务，任务如下：

六道对错题，对 4 道以上晋级。

```java
String hello="hello world!"; 
String hello1=new String("hello world!");
System.out.println(hello==hello1); //1
String hello2="hello world!";
System.out.println(hello==hello2); //2
String append="hello"+" world!";
System.out.println(hello==append); //3
final String pig = "length: 10";
final String dog = "length: " + pig.length();
System.out.println(pig==dog); //4
final String dog1 = ("length: " + pig.length()).intern();
System.out.println(pig==dog1); //5
System.out. println("Animals are equal: "+ pig == dog);//6
```

很快成绩出来了，小胖对了 6 道题目，顺利晋级！小白对了 5 道题目，顺利晋级！

小L针对他们各自的题目和他们一起做了点评。

小胖的题目正确答案为：

```java
true
true
false
true
true
true
false
true
```

小胖全部选择的 true，其中错误的两道题目如下：

```java
Integer num3=34567;
Integer num4=34567;
System.out.println(num3==num4);
Integer num23=-34567;
Integer num24=-34567;
System.out.println(num23==num24);	
```

**小L：**“小胖，你知道这两道题目为什么是 false 吗？”

**小胖：**“不知道，我平时写程序认为 equals 和 == 都是相等，就全部选择 true 了。”

**小L：**“首先，== 指的是`对象`的比较，equal 是`值`的比较；其次，Integer 类型的赋值表达式 = 有一个装箱的过程，在装箱时先对 int 得值做判断，如果值在【-128，127】之间时返回创建好的对象，否则返回新的对象，JLS5 有介绍，源码里也有实现”

```java
 /**
 * Returns an {@code Integer} instance representing the specified
 * {@code int} value. If a new {@code Integer} instance is not
 * required, this method should generally be used in preference to
 * the constructor {@link #Integer(int)}, as this method is likely
 * to yield significantly better space and time performance by
 * caching frequently requested values.
 *
 * This method will always cache values in the range -128 to 127,
 * inclusive, and may cache other values outside of this range.
 *
 * @param i an {@code int} value.
 * @return an {@code Integer} instance representing {@code i}.
 * @since 1.5
 */
 public static Integer valueOf(int i) {
 if (i >= IntegerCache.low && i <= IntegerCache.high)
 return IntegerCache.cache[i + (-IntegerCache.low)];
 return new Integer(i);
 }
```

小胖：“知道了！”

小L：“小白，你的表现超出我的预期，对了 5 道题目，特别是最后一题，能答对，基础很扎实。第四题做错的原因是：通过常量表达式运算得到的字符串是在**编译时**计算得出的，并且之后会将其当作字符串常量对待；在**运行时**通过连接运算得到的字符串是新创建的，因此要区别对待。其它如可以通过显示的限定运算得到的字符串为字符串常量，String.intern 方法可以"限定，操作符的优先级也可以通过 JLS（Java language Specification） Java编程规范这本书中可以查到。”

![img](image/1170d8d6e3f6457aa1da18ab5e434411.jfif)

小L：“再总结一下，不管是 Integer、Long、Char、Short、Double、Float、String 都涉及到一个拆箱装箱的过程，各自的规则也略有差异，故要仔细看看 JLS 的定义”。

此时，小 L 心中已经有了主意，知道谁才是他们组的优秀员工。你猜是谁呢？