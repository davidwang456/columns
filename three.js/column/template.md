# 第X章：{{章节标题}}

## 元信息

- 学习目标：
- 先修要求：
- 预估时长：
- 最终产出：
- 适用角色（开发/测试/运维）：

---

## 1. 项目背景（约500字）

### 1.1 业务场景

用一个真实或拟真的业务需求引出本章主题，例如：
- 智慧园区告警看板
- 设备状态三维巡检
- 大屏多场景联动展示

### 1.2 痛点放大

如果没有本章技术，通常会出现：
- 性能问题（卡顿、掉帧、首屏慢）
- 一致性问题（状态不同步、显示不一致）
- 可维护性问题（场景逻辑耦合、扩展困难）

### 1.3 流程图（可选）

```mermaid
flowchart LR
    businessNeed[业务需求输入] --> techChoice[技术选型]
    techChoice --> solution[Three.js 场景方案]
    solution --> delivery[上线交付]
```

---

## 2. 项目设计（剧本式交锋对话，约1200字）

> 固定角色：小胖（生活化比喻）、小白（深度追问）、大师（技术决策）

### 2.1 第一轮：小胖开球

小胖：  
“{{生活化问题抛出，1-2轮}}”

小白：  
“{{追问原理或边界条件}}”

大师：  
“{{先讲业务约束，再给初步解法}}”

技术映射：  
“{{把生活比方映射为具体技术术语}}”

### 2.2 第二轮：小白深入质疑

小白：  
“{{追问失败场景、替代方案、风险}}”

小胖：  
“{{补充生活视角疑问}}”

大师：  
“{{比较方案优劣并明确选型}}”

技术映射：  
“{{生活比方 -> 架构/算法/工程术语}}”

### 2.3 第三轮：大师收束并引出下一子话题

小胖：  
“{{对成本、复杂度的直觉疑问}}”

小白：  
“{{关注可观测性、可测试性、可演进性}}”

大师：  
“{{给出最终设计，说明为何可扩展}}”

技术映射：  
“{{本章核心概念总结映射}}”

---

## 3. 项目实战（约1500-2000字）

### 3.1 环境准备

- Node.js：建议 LTS（如 20.x）
- 包管理器：npm / pnpm
- Three.js：与项目锁定版本一致
- 构建工具：Vite + TypeScript（推荐）

示例命令：

```bash
npm create vite@latest chapter-demo -- --template vanilla-ts
cd chapter-demo
npm install three
npm run dev
```

### 3.2 分步实现

#### 步骤1：{{步骤目标一句话}}

代码片段（可运行、带注释）：

```ts
// 示例：初始化场景、相机、渲染器
import * as THREE from "three";

const scene = new THREE.Scene();
const camera = new THREE.PerspectiveCamera(60, window.innerWidth / window.innerHeight, 0.1, 1000);
camera.position.set(0, 3, 8);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setSize(window.innerWidth, window.innerHeight);
document.body.appendChild(renderer.domElement);
```

运行结果（文字描述）：
- 页面渲染成功，看到默认背景和相机视野

常见坑与解决：
- 黑屏：检查相机位置和 near/far
- 拉伸：监听窗口变化并更新相机纵横比

#### 步骤2：{{步骤目标一句话}}

代码片段（可运行、带注释）：

```ts
// 在这里补充本章核心功能代码
```

运行结果（文字描述）：
- {{预期结果}}

常见坑与解决：
- {{坑1 -> 解法}}
- {{坑2 -> 解法}}

#### 步骤3：{{步骤目标一句话}}

代码片段（可运行、带注释）：

```ts
// 在这里补充本章高级或收尾代码
```

运行结果（文字描述）：
- {{预期结果}}

常见坑与解决：
- {{坑1 -> 解法}}

### 3.3 完整代码清单

- 仓库地址：{{Git 仓库链接}}
- 目录说明：
  - `src/main.ts`：入口
  - `src/scene/*`：场景模块
  - `src/system/*`：渲染、资源、事件系统

### 3.4 测试验证

可选验证方式：
- 单元测试（Vitest/Jest）
- 交互验证脚本
- 命令行检查输出

示例：

```bash
npm run test
npm run build
```

---

## 4. 项目总结（约500-800字）

### 4.1 优点 & 缺点（建议表格）

| 维度 | 优点 | 缺点 |
| --- | --- | --- |
| 性能 | {{优点}} | {{缺点}} |
| 工程复杂度 | {{优点}} | {{缺点}} |
| 可维护性 | {{优点}} | {{缺点}} |

### 4.2 适用场景

适用：
1. {{场景1}}
2. {{场景2}}
3. {{场景3}}

不适用：
1. {{场景A}}
2. {{场景B}}

### 4.3 注意事项

- 配置陷阱：
- 版本兼容：
- 安全边界：

### 4.4 常见踩坑经验（3例）

1. 故障案例一：{{现象 + 根因 + 修复}}
2. 故障案例二：{{现象 + 根因 + 修复}}
3. 故障案例三：{{现象 + 根因 + 修复}}

### 4.5 思考题（2道）

1. {{进阶题1}}
2. {{进阶题2}}

> 答案放在下一章或附录。

### 4.6 推广计划提示（跨部门）

- 开发关注：架构、原理、源码实现、可扩展性
- 测试关注：关键路径、回归基线、自动化脚本
- 运维关注：监控指标、容量预估、发布回滚
- 建议阅读顺序：开发先读背景与设计，测试并行读实战，运维从中级篇开始全程参与
