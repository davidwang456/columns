# 第26章：流量镜像与 A/B 测试

> 源码关联：src/http/modules/ngx_http_mirror_module.c、src/http/modules/ngx_http_split_clients_module.c
> 批次说明：第 2 批（精修版 2/5）

**关键词摘要**：mirror、split_clients、A/B 测试、灰度发布、影子流量

---

## 1. 项目背景

推荐模型准备灰度，新模型离线评估很好，但团队担心线上真实流量分布与离线样本差异巨大。为了降低发布风险，架构组决定先镜像再分桶，做到“先看效果，再放流量”。

这类问题在中级阶段非常典型：指标表面好看，但业务体验在关键节点失真。团队过去依赖“经验调参”，现在必须切换为“指标驱动 + 故障演练 + 可回滚”的交付模式。
本章会沿着“真实业务痛点 -> 方案博弈 -> 逐步落地 -> 验证闭环”来推进，确保这套方案不仅能跑，还能在高峰与异常中站得住。

---

从知识路径上看，本章与上一章《WebSocket 与长连接管理》形成前后呼应：上一章解决的是“局部能力成立”，本章要进一步回答“在真实流量和复杂协作下如何稳定成立”。

## 2. 项目设计

**场景**：源码共读会，三人一边看调用栈一边对照线上日志。

**小胖**：要不先临时加机器顶住，等活动结束再说？

**小白**：临时兜底可以，但没有边界验证，下次还会复发。

**大师**：我们先把讨论框架对齐成三个层次：
1. 机制层：问题为什么发生？
2. 策略层：参数如何选？边界在哪里？
3. 工程层：怎么验证、怎么回滚、怎么复盘？

**大师（技术映射）**：把系统当航班调度。平峰时人人都能飞，暴雨天还能准点落地，才叫真正的系统能力。

**小胖**：那我们这次怎么确保结论可信？

**大师**：坚持“三段证据链”：
- 基线数据：不改配置的真实表现；
- 实验数据：单变量变更后的收益与副作用；
- 异常数据：故障注入后的稳定性表现。

**小白**：另外要把结论写成可执行规则，比如“超过阈值自动降级/切流”，不要停留在口头共识。

**大师（技术映射）**：工程化不是“我知道”，而是“团队都能复现同样结果”。

---

## 3. 项目实战

### 环境准备

- 系统：Ubuntu 22.04（或同等发行版）
- Nginx：1.31.0
- 工具：`curl`、`wrk`、`ss`、`jq`
- 附加工具：第 24 章建议 `mysql` / `dig`，第 25 章建议 `websocat`

```bash
sudo nginx -V
sudo nginx -t
```

### 步骤一：最小配置落地

**步骤目标**：先建立可运行基线，避免“边改边猜”。

```nginx
split_clients "$request_id$remote_addr" $ab_group {
    5%  canary;
    *   stable;
}
server {
    listen 8080;
    location /recommend {
        mirror /_mirror_recommend;
        proxy_set_header X-Ab-Group $ab_group;
        proxy_pass http://rec_$ab_group;
    }
    location = /_mirror_recommend {
        internal;
        proxy_method POST;
        proxy_set_header X-Mirror-Request 1;
        proxy_pass http://rec_shadow;
    }
}
```

**运行结果（预期）**：
- 配置校验通过；
- 关键接口连通；
- `error.log` 无持续性异常。

**可能遇到的坑**：仅用功能联调代替压测与异常演练，导致上线后才暴露问题。

### 步骤二：基线压测与指标采集

**步骤目标**：用可比较的数据判断方案优劣。

```bash
for i in {1..50}; do curl -s http://127.0.0.1:8080/recommend > /dev/null; done
rg "X-Ab-Group" /var/log/nginx/access.log
```

**建议关注指标**：
- 吞吐：QPS / TPS
- 延迟：P95、P99
- 稳定性：错误率、超时率、重试率
- 资源：活动连接、CPU、内存

**可能遇到的坑**：压测时间太短，掩盖连接池、缓存、GC 等“慢热型问题”。建议最少跑 5~10 分钟稳定段。

### 步骤三：故障注入与回滚演练

**步骤目标**：验证系统在异常下仍可控。

```bash
# 触发变更
sudo nginx -t && sudo nginx -s reload
# 回滚示例
sudo cp /etc/nginx/nginx.conf.bak /etc/nginx/nginx.conf
sudo nginx -t && sudo nginx -s reload
```

**故障注入建议**：
1. 下线一个后端节点；
2. 人工增加后端延迟（如 tc 注入）；
3. 叠加突发流量观察尾延迟。

**本章实战目标**：落地镜像 + A/B 双轨发布，形成低风险上线流程。

### 完整代码清单

- 建议目录：`ops/nginx/ch26/`
- 建议文件：`nginx.conf`、`upstream.conf`、`bench.sh`、`fault_inject.sh`、`rollback.sh`
- 结果沉淀：`result.md`（记录参数、数据、结论、回滚点）

---

### 测试验证

```bash
# 基础连通性验证（示例）
curl -i http://127.0.0.1:8080/health
```

验证要点：
- 关键接口返回码符合预期（2xx/4xx/5xx与设计一致）；
- 关键日志字段完整（如 request_id、upstream 耗时、状态码）；
- 在小流量压测下无异常错误峰值。

## 4. 项目总结

### 优点与缺点

| 维度 | 方案优势 | 潜在代价 |
|---|---|---|
| 可用性 | 有明确保护与回滚机制，异常可控 | 建设初期流程更重 |
| 性能 | 能在真实指标上做持续优化 | 需要持续压测资源 |
| 协作 | 跨团队共享同一评估标准 | 对执行纪律要求更高 |

### 适用场景

1. 高峰波动明显、对可用性要求高的业务。
2. 网关承担统一流量治理职责的平台。
3. 希望建立“可复盘、可复制”优化流程的团队。

**不适用场景**：
- 单机低流量且无明显增长预期的项目。
- 缺少监控与压测基础设施、无法闭环验证的团队。

### 注意事项

1. 指标先行：没有基线就没有优化结论。
2. 变更最小化：一次只改一个关键变量。
3. 回滚可执行：任何优化都必须自带止损路径。

### 常见踩坑经验

- 只追求命中率/QPS，不看业务正确性。
- 缺少异常注入，线上首遇故障才发现边界。
- 变更记录缺失，后续团队无法继承经验。

### 推广计划提示

- **开发团队**：先复用本章最小配置与脚本，按“单变量”方式做参数实验并沉淀变更记录。
- **测试团队**：优先补齐异常路径用例（超时、重试、限流、故障转移），并固化回归清单。
- **运维团队**：将监控阈值、告警策略与回滚脚本纳入发布流程，确保高峰期可快速止损。
- **协作顺序建议**：开发先完成方案基线 -> 测试做功能/压力/故障验证 -> 运维执行灰度与上线守护。

### 思考题

1. 如果本章只能保留一条保护机制，你会留哪条？为什么？
2. 当性能收益与稳定性收益冲突时，你会如何做发布决策？

---

在阅读顺序上，建议你先完成本章的最小实验与故障演练，再进入《监控体系与 Prometheus 集成》。这样能把“配置会写”升级为“结论可验证、变更可回退”的工程能力。

> **下一章预告**：监控体系与 Prometheus 集成
