# 第12章：专栏总结-路线图、认证与社区资源

> 本章目标字数：3000–5000。统一环境见 [ENV.md](../ENV.md)。

> **版本**：ROS 2 Humble（Ubuntu 22.04，统一环境见 [ENV.md](../ENV.md)）
> **定位**：高级篇 · 面向架构师与资深开发，强调源码边界、极端场景与生产取舍。
> **前置阅读**：建议先完成基础篇与中级篇相关章节，尤其关注执行器、QoS、Launch、观测性与 SRE。
> **预计阅读**：45 分钟 | 实战耗时：90–150 分钟

## 1. 项目背景

### 业务场景

读者完成 **38** 章后，可按岗位深化：**感知工程师** → **OpenCV + PCL + deep learning pipeline**；**导航工程师** → **Nav2 插件开发 + 行为树**；**系统工程师** → **Yocto + 实时 + SRE**。本书止于「**ROS 2 通用底盘**」，垂直领域需继续专题书。

### 痛点放大

1. **路线过宽**：同时学 Nav2、MoveIt2、DDS、嵌入式与视觉，最后没有一个可演示作品。
2. **能力不可证明**：只收藏教程，没有 README、CI、bag、截图与复盘文档，面试或述职无法复现。
3. **忽略岗位差异**：开发、测试、运维与架构师需要的深度不同，若不分层推进会浪费时间。

---

### 业务指标与交付边界

本章不追求“把所有概念一次讲完”，而是交付一个可复现的工程切片：

1. **可运行**：至少有一组命令、脚本或配置能够在 Humble 环境中执行。
2. **可观察**：运行后能用 `ros2` CLI、日志、RViz、rosbag2 或系统工具看到明确现象。
3. **可交接**：读者能把 **专栏总结-路线图、认证与社区资源** 的关键假设、输入输出、失败模式写进项目 README 或排障手册。

**本章交付目标**：完成一个围绕 **专栏总结-路线图、认证与社区资源** 的最小闭环，并留下可复盘的命令、截图或日志证据。

## 2. 项目设计

### 总体架构图

```mermaid
flowchart LR
  requirement[业务需求] --> concept["专栏总结-路线图、认证与社区资源"]
  concept --> config[配置与代码]
  config --> runtime[运行时观测]
  runtime --> verify[测试验证]
  verify --> runbook[交付与复盘]
```

这张图用于对齐 `example.md` 的“端到端项目链路”写法：先从业务需求出发，再落到配置/代码，最后用观测与验收把结论闭环。

### 剧本对话

**小胖**：学完能去考啥证？我妈问起来我总不能说「会 spin」吧？

**小白**：招聘 JD 写「熟悉 ROS2」、面试却问 **DDS 域** 和 **executor**，我们这本读完算啥水平？

**大师**：ROS **没有全球统一考级**。企业更认三件事：**可复现仓库**（固定发行版、README 一键起）、**公开 Issue/PR**（证明协作）、**作品集**能讲清**约束→架构→权衡**（Nav2、硬件、网络各占多少坑）。**特种机器人或功能安全**认证属于另一条线，和「会调 Nav2」不是包含关系——需要**流程与证据链**，本书给你的是**技术底座**。

**技术映射 #1**：**能力证明** = **工件（artifact）** + **可追溯决策**。

---

**小胖**：那我下一步刷 **MoveIt2** 还是 **Gazebo** 还是直接找工作实习？

**大师**：按岗位切：**感知**就深挖 **sensor + TF + 标定 + 数据**；**移动机器人**就 **Nav2 插件 + 系统集成**；**嵌入式**就 **micro-ROS / 刷写 / 实时**（**A07/A09**）。别同时开五条线——用**一条垂直切片**做到「能给别人 clone 跑」比收藏 fifty 个 launch 强。

**技术映射 #2**：**T 型能力**：一竖（可演示项目）+ 一横（本书通识）。

---

**小白**：**社区**里吵架最多的话题是啥？我们要站队吗？

**大师**：常有 **Fast-DDS vs Cyclone**、**Python vs C++**、**Launch XML vs Python**——多数是**场景变量不同**。成熟团队输出的是：**我们在何种网络/硬件/团队约束下选了 A，代价是 X**。站队不如**写清决策记录（ADR）**。

**技术映射 #3**：**Discourse + REP** 是「标准战场」，读结论前的**假设**。

---

**大师**：若真想「背书」，可考虑 **厂商培训 / 高校慕课证书** 作为辅助，但面试官仍会用白板追问 **QoS 与 TF**——那一关本书已经反复操练了（**B05/B09/M02**）。

---

## 3. 项目实战

**结业项目**：按 B04 结构落地。

> 本章无「实验室代码」，而是把 **环境准备 → 分步 → 清单 → 验收** 固化为 **你个人的 30 天路线**；与 [B04](第16章：话题与消息-发布订阅第一印象.md) 同级结构，便于写入 **简历/述职**。

### 环境准备

- **机器**：**Ubuntu 22.04 + ROS 2 Humble**（[ENV.md](../ENV.md)）。
- **账号**：**GitHub/GitLab** 可建公开或**可见范围**仓库；**CI 分钟数**自行评估。
- **时间**：建议 **≥20h** 有效编码（**含**联调与文档）。

**项目目录结构**（建议随章落地到自己的工作区）：

```text
ros2_ws/
  src/
    专栏总结_路线图_认证与社区资源/
      package.xml
      launch/
      config/
      scripts/
      test/
  docs/
    runbook.md      # 记录命令、预期输出、截图或日志
```

说明：若本章以阅读源码、配置或运维演练为主，可以把 `scripts/` 换成 `notes/`，但仍建议保留 `config/` 与 `test/`，方便后续复盘。

### 分步实现

#### 步骤 1：选题 — 一条垂直切片

- **目标**：从下列 **选一**（勿多选）：
  1. **移动底盘**：**Nav2** 最小仿真 + **一张自定义地图** + **记录 bag**；或
  2. **机械臂/抓手**：**MoveIt 2** hello（若硬件无，仅仿真）；或
  3. **工具向**：为本仓库 **38 章** 做 **`markdown` 校验脚本** + **GitHub Action**（非 ROS 但可证明工程化）。
- **预期输出**：**`README.md` 一句话电梯陈述** + **约束列表**（传感器、算力、网络）。
- **坑与解法**：范围蔓延 —— **先跑通再美化**。

#### 步骤 2：仓库骨架

- **目标**：**README** 具备 **「一键 reproduce」** 段落（与 [A08](第08章：构建系统深入-ament、依赖与私有仓库.md) 社会工程对齐）。
- **命令/结构**：

```text
your-project/
  README.md          # 必须：环境、安装、运行、测试
  .github/workflows/ci.yaml
  ros2_ws/src/...    # 若选 ROS
```

- **预期输出**：**clone → install → run** 三步可被**陌生人**执行（你自己 **48h 后 cold run** 测一次）。
- **坑与解法**：**漏写 `source install/setup.bash`** —— README 显式写出。

#### 步骤 3：最小 CI

- **目标**：**`colcon build && colcon test`**（ROS）或 **`pytest`/`shellcheck`**（工具向）。
- **命令（GitHub Actions 示意）**：

```yaml
# .github/workflows/ci.yml — 骨架，按需补 ROS 容器
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - run: |
          sudo apt update
          sudo apt install -y ros-humble-desktop python3-colcon-common-extensions
          echo "在此 source 并 colcon build"
```

- **预期输出**：**CI 绿**；失败时能 **log 定位**。
- **坑与解法**：**无测试** → 至少 **`launch_test` smoke** 或 **`ros2 doctor`**（[A10](第10章：SRE-故障演练、SLA、告警与回滚.md)）。

#### 步骤 4：复盘文章 / ADR

- **目标**：**一篇公开 blog** 或 **一条 ADR**：**约束 → 取舍 → 未解决问题**（剧本）。
- **预期输出**：**URL** 或 **`docs/adr/0001-....md`**。
- **坑与解法**：只贴代码无叙事 —— **招聘方无法评估思维**。

### 完整代码清单

- **你的仓库 URL**（完成后填入简历）。
- **依赖**：固定 **`ros-humble-*` 包列表** 或 **Dockerfile digest**。

### 交付物清单

- **README**：说明 **专栏总结-路线图、认证与社区资源** 的业务背景、运行命令、预期输出与常见失败。
- **配置/代码**：保留本章涉及的 launch、YAML、脚本或源码片段，避免只存截图。
- **证据材料**：至少保留一份终端输出、RViz 截图、rosbag2 片段、trace 或日志摘录。
- **复盘记录**：记录“为什么这样配置”，尤其是 QoS、RMW、TF、namespace、安全和性能相关取舍。

### 测试验证

- **结业标准（自检表）**：
  - [ ] **他人可读 README 复现**；
  - [ ] **CI 绿**；
  - [ ] **至少 1 条 issue/PR**（协作痕迹）或 **导师 review 记录**；
  - [ ] 能 **白板讲清**：**DDS 域**（[M01](第26章：DDS 发现、域（Domain）与跨机通信.md)）+ **QoS**（[M02](第27章：QoS 深度-history、deadline、durability.md)）+ **Executor**（[A01](第01章：rcl-rclcpp 执行模型与源码导读.md)）三者之一 **10 分钟**。

### 社区资源（入口）

- [docs.ros.org](https://docs.ros.org) — 权威文档  
- [answers.ros.org](https://answers.ros.org) — 问答  
- **ROS Discourse** — 设计讨论  
- **Navigation2**、**MoveIt 2** 独立文档  

### 验收清单

- [ ] 能在干净终端重新 `source /opt/ros/humble/setup.bash` 后复现本章命令。
- [ ] 能指出 **专栏总结-路线图、认证与社区资源** 的核心输入、输出、关键参数与失败边界。
- [ ] 能把至少一条失败案例写成“现象 → 排查命令 → 根因 → 修复”的四段式记录。
- [ ] 能说明本章内容与相邻章节的依赖关系，避免把单点技巧误当成系统方案。

---

## 4. 项目总结

### 优点与缺点

| 维度 | 说明 |
|------|------|
| 覆盖 | 基础→中级→高级链路 |
| 局限 | 领域算法深度需另书 |

### 适用场景

- 团队需要把 **专栏总结：路线图、认证与社区资源** 从个人经验沉淀为可复用流程。
- 新人、测试与运维需要用同一套命令与术语对齐问题现象。
- 项目进入联调阶段，需要记录参数、话题、日志与验收结果。
- 需要为后续源码阅读、性能优化或生产复盘提供上下文。

### 不适用场景

- 只做一次性演示且不需要交接、回归或复盘的临时脚本。
- 现场约束尚未明确时，不宜把 **专栏总结：路线图、认证与社区资源** 的示例参数直接当作生产标准。

### 注意事项

- **版本兼容**：所有命令以 Humble 与 [ENV.md](../ENV.md) 为基线，其他发行版需查 `--help` 与官方文档。
- **配置边界**：不要把实验参数直接带入生产；先记录硬件、RMW、QoS、网络与时钟条件。
- **安全边界**：涉及远程调试、容器权限、证书或硬件接口时，先按最小权限原则收敛。

### 常见踩坑经验

1. **只看现象不记录环境**：同一命令在不同 RMW、Domain、QoS 或硬件上结果不同。根因通常是缺少版本与环境快照。
2. **一次改多个变量**：参数、Launch、网络与代码同时变化，导致无法归因。解决方法是每次只改一项并保存日志或 bag。
3. **忽略跨角色交接**：开发能跑通但测试/运维无法复现。根因是缺少最小验收命令、预期输出与失败处理路径。

### 思考题

1. 你最想优先落地的 **一个** 生产改进点是什么？写出 **SLO** 与 **验证方法**。
2. 若向团队推荐 **ROS 2 Jazzy** 升级，列出 **风险矩阵**。

**答案**：开放式，见 [APPENDIX A12](../APPENDIX-answers.md#a12)。

### 推广计划提示

- **全员**：保留 [INDEX.md](../INDEX.md) 为「地图」。
- **新人**：B01–B13 复盘小测。
- **架构师**：高级篇 + **RoboStack** / **micro-ROS** 扩展阅读。

---

**导航**：[上一章：A11](第11章：生产案例复盘（综合）-导航栈调优一页纸方法论.md) ｜ [总目录](../INDEX.md) ｜ 全书终

> **本章完**。你已经完成 **专栏总结-路线图、认证与社区资源** 的端到端学习：从业务场景、设计对话、实战命令到验收清单。下一步建议把本章交付物纳入自己的 ROS 2 工作区，并在后续章节中持续复用同一套 README、配置和测试记录方式。
