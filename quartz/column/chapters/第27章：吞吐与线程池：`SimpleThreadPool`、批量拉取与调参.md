# 第27章：吞吐与线程池：`SimpleThreadPool`、批量拉取与调参

> **篇别**：中级篇  
> **建议篇幅**：3000–5000 字（含对话与代码）  
> **结构约束**：对齐 [专栏模板](../template.md) 四段式。

## 示例锚点

| 类型 | 路径 |
| --- | --- |
| example11 | [LoadExample.java](../../examples/src/main/java/org/quartz/examples/example11/LoadExample.java) |
| 源码 | [SimpleThreadPool.java](../../quartz/src/main/java/org/quartz/simpl/SimpleThreadPool.java) |

## 1 项目背景（约 500 字）

### 业务场景

大促需要 **每秒触发上百个短 Job**，DBA 担心 **`acquire` SQL 太频繁**，运维担心 **线程上下文切换过多**。`LoadExample` 用于观察 **高负载下调度行为**；同时 `quartz.properties` 支持 **`batchTriggerAcquisitionMaxCount`**（键名以版本文档为准）等参数，调节 **一次拉取触发器数量** 与 **延迟/DB 压力** 的折中。

### 痛点放大

- **threadCount 盲目增大**：DB 连接与锁竞争恶化。
- **batch 过大**：单次 `acquire` 时间长，**尾延迟**上升。
- **Job 执行时间长尾**：与短周期 Trigger 叠加 misfire。

## 2 项目设计（约 1200 字）

**角色**：小胖 · 小白 · 大师

---

**小胖**：线程开 200，调度是不是飞快？

**小白**：`SimpleThreadPool` 和 Java `ExecutorService` 差在哪？

**大师**：`SimpleThreadPool` 是 Quartz **自带的朴素池**，满足 **SPI `ThreadPool` 契约**；它像 **「固定人数的后厨」**——不是越大人越快，**出菜速度还受洗碗池（DB）和灶台（CPU）限制**。

**技术映射**：**`org.quartz.threadPool.class`**。

---

**小胖**：批量拉触发器像一次买一周菜？

**小白**：batch 大了，会不会拉到「暂时不该执行」的？

**大师**：调度线程会按 **可用性与时间窗** 过滤；batch 调优是 **「少跑 DB vs 单次多拿」** 的权衡。要像 **超市补货**：补太少 **频繁跑仓库**；补太多 **冷柜塞爆**。

**技术映射**：**`org.quartz.scheduler.batchTriggerAcquisitionMaxCount`**（以文档为准）。

---

**小胖**：LoadExample 我能直接压生产库吗？

**小白**：如何读 `SchedulerMetaData` 判断瓶颈？

**大师**：压测只在 **影子库/预发**；线上用 **指标**：`jobsExecuted`、`misfire` 计数、**DB 慢查询**、**线程池 active 数**。

**技术映射**：**metrics + tracing**。

---

**小胖**：这跟食堂打饭有啥关系？我就想把任务跑起来。

**小白**：那 **谁来背锅**：触发没发生、发生了两次、还是延迟太久？指标口径先定死。

**大师**：把 **Scheduler 当「编排台」**：Job 是工序，Trigger 是节拍，Listener 是质检；节拍错了，工序再快也白搭。

**技术映射**：**可观测性口径 + Job／Trigger 职责边界**。

---

**小胖**：配置一多我就晕，`quartz.properties` 到底哪些能碰？

**小白**：**线程数、misfireThreshold、JobStore 类型** 改了会不会让 **同一套代码** 在预发与生产行为不一致？

**大师**：做一张 **「配置变更矩阵」**：改一项就写清 **影响面、回滚方式、验证命令**；RAM 与 JDBC 不要混着试。

**技术映射**：**显式配置治理 + 环境一致性**。

---

**小胖**：我本地跑得飞起，一上集群就「偶尔不跑」。

**小白**：**时钟漂移、数据库时间、JVM 默认时区** 三者不一致时，**nextFireTime** 你怎么解释给业务？

**大师**：把 **时区写进契约**：服务器、Cron、业务日历 **同一基准**；日志里同时打 **UTC 与业务时区**。

**技术映射**：**时区／DST 与触发语义**。

---

**小胖**：Trigger 优先级是不是数字越大越牛？

**小白**：**饥饿**怎么办？低优先级永远等不到的话，SLA 谁负责？

**大师**：优先级是 **「同窗口抢锁」** 的 tie-breaker，不是万能插队票；该 **拆分队列** 的别硬挤一个 Scheduler。

**技术映射**：**Trigger 优先级与吞吐隔离**。

---

**小胖**：misfire 不就是晚了吗，晚跑一下不行？

**小白**：**合并、丢弃、立即补偿** 三种策略对 **资金类任务** 分别是啥后果？

**大师**：把 **业务幂等键** 与 **misfireInstruction** 绑在一起评审；没有幂等就别选「立刻全部补上」。

**技术映射**：**misfire 策略与业务一致性**。

---

**小胖**：`JobDataMap` 里塞个大 JSON 爽不爽？

**小白**：**序列化成本、版本升级、跨语言** 谁来买单？失败重试会不会把 **半截状态** 写回去？

**大师**：**小键值 + 外置大对象**；必须进 Map 的，**版本字段** 与 **兼容读** 写进规范。

**技术映射**：**JobDataMap 体积与演进策略**。

---

**小胖**：`@DisallowConcurrentExecution` 一贴我就安心了。

**小白**：**同 JobKey 串行** 会不会把 **补偿触发** 堵成长队？线程池够吗？

**大师**：先画 **并发模型草图**：哪些 Job 必须串行、哪些只是 **资源互斥**（应改用锁或分片）。

**技术映射**：**并发注解与队列时延**。

---

**小胖**：关机我直接拔电源，反正有下次触发。

**小白**：**在途 Job** 写了一半的外部副作用怎么算？**at-least-once** 下会不会双写？

**大师**：发布路径默认 **`shutdown(true)` + 超时**；`kill -9` 只能进 **混沌演练**，不进 **常规 Runbook**。

**技术映射**：**优雅停机与副作用幂等**。

---

**小胖**：Listener 里写业务逻辑最快了。

**小白**：Listener 异常会不会 **吞掉主流程** 或 **拖慢线程**？顺序保证吗？

**大师**：Listener 只做 **旁路观测与轻量编排**；重逻辑回 **Job** 或 **下游消息**。

**技术映射**：**Listener 边界与失败隔离**。

---

**小胖**：JDBC JobStore 不就是多几张表吗？

**小白**：**行锁、delegate、方言、索引** 哪个没对齐会出现 **幽灵触发** 或 **长时间抢锁**？

**大师**：把 **DB 监控**（慢查询、锁等待）与 **Quartz 线程栈** 对齐看；调参前先 **确认隔离级别与连接池**。

**技术映射**：**持久化 JobStore 与数据库协同**。

---

**小胖**：集群一开我就加节点，TPS 一定涨吧？

**小白**：**抢锁成本、心跳、instanceId** 乱配时，会不会 **越加越慢**？

**大师**：用 **压测曲线** 证明拐点；集群收益来自 **HA 与横向扩展边界**，不是魔法按钮。

**技术映射**：**集群伸缩与锁竞争**。

---

**小胖**：我想自定义 ThreadPool 秀一把。

**小白**：线程工厂、拒绝策略、上下文传递（MDC）**漏一项** 会出现啥线上症状？

**大师**：自定义可以，但要 **对齐 SPI 契约**与 **关闭语义**；否则 **泄漏线程** 比默认池更难查。

**技术映射**：**ThreadPool SPI 与生命周期**。

---

**小胖**：这跟食堂打饭有啥关系？我就想把任务跑起来。

**小白**：那 **谁来背锅**：触发没发生、发生了两次、还是延迟太久？指标口径先定死。

**大师**：把 **Scheduler 当「编排台」**：Job 是工序，Trigger 是节拍，Listener 是质检；节拍错了，工序再快也白搭。

**技术映射**：**可观测性口径 + Job／Trigger 职责边界**。
## 3 项目实战（约 1500–2000 字）

### 环境准备

运行 [LoadExample.java](../../examples/src/main/java/org/quartz/examples/example11/LoadExample.java)，先 **小并发** 再放大。

### 分步实现

**步骤 1：目标** —— 记录 **不同 `threadCount`** 下的 **吞吐与 misfire**。

**步骤 2：目标** —— 调整 **batch 参数**（若版本支持），对比 **DB QPS**。

**步骤 3：目标** —— 打印 `SchedulerMetaData`：`numJobsFired`、`runningSince` 等。

### 可能踩坑

| 坑 | 解决 |
| --- | --- |
| 压测打挂共享 dev DB | 独立实例 |
| 只看 CPU 不看 DB | 全链路 |
| 忽略 GC | 分配速率分析 |

### 完整代码清单

- [LoadExample.java](../../examples/src/main/java/org/quartz/examples/example11/LoadExample.java)

### 测试验证

JMH 或简单 for 循环 + 限流，输出 **p50/p95/p99** 触发延迟。

## 4 项目总结（约 500–800 字）

### 优点与缺点（对比同类技术）

| 维度 | SimpleThreadPool | 自定义 Executor SPI |
| --- | --- | --- |
| 上手 | 易 | 难 |
| 弹性 | 弱 | 强 |

### 适用 / 不适用场景

- **适用**：绝大多数部署。
- **不适用**：需要 **动态伸缩池** 且强定制。

### 注意事项

- **线程名前缀**：便于 thread dump。
- **上下文类加载器**：容器环境。

### 常见踩坑（生产案例）

1. **threadCount 与 DB 连接不匹配**：第23章。
2. **batch 误配**：根因是无压测。
3. **长任务占满池**：根因是无隔离。

#### 第26章思考题揭底

1. **`requestsRecovery` 影响**  
   **答**：当 Job **执行中崩溃且被标记为需恢复** 时，集群其他节点在 **认领 `FIRED_TRIGGERS` / 恢复逻辑** 时会更倾向 **重新调度该 Job 的执行**（语义以官方文档与版本为准）。用于 **避免静默丢失在途任务**。

2. **有状态 Job recovery 注意**  
   **答**：必须把 recovery 视为 **至少一次执行**；**JobDataMap 回写与业务表** 可能 **部分完成**；需要 **幂等键**、**分段提交**、**可重放日志**；并与 **`@DisallowConcurrentExecution`** 协同避免 **并发写状态**。

### 思考题（答案见下一章或 [答案索引](answers-index.md)）

1. `batchTriggerAcquisitionMaxCount` 调大对 DB 与延迟分别有什么影响？
2. `LoadExample` 对生产的启示是什么？

### 推广计划提示

- **测试**：容量曲线。
- **运维**：线程与连接池联合看板。
- **开发**：下一章 RMI 与安全。
