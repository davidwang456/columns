# 第21章：Trigger 优先级与饥饿

> **篇别**：中级篇  
> **建议篇幅**：3000–5000 字（含对话与代码）  
> **结构约束**：对齐 [专栏模板](../template.md) 四段式。

## 示例锚点

| 类型 | 路径 |
| --- | --- |
| example14 | [PriorityExample.java](../../examples/src/main/java/org/quartz/examples/example14/PriorityExample.java) |

## 1 项目背景（约 500 字）

### 业务场景

大促期间 **「库存熔断检查」** 每 2 秒必须跑一次，而 **「报表归档」** 每 5 分钟跑一次即可。线程池只有 4 个工作线程，若两者 **同时到期**，希望 **熔断检查优先获得执行槽位**。Quartz 提供 **`TriggerBuilder.withPriority(int)`**（默认 `5`，具体默认值以文档为准），在 **资源争用** 时提供 **同 Scheduler 内的相对顺序提示**。

### 痛点放大

- **误以为优先级跨 JVM**：集群下各实例仍 **各自调度**。
- **全部设为最高**：等于 **没有优先级**。
- **低优任务饥饿**：长期得不到线程，**misfire 堆积**。

## 2 项目设计（约 1200 字）

**角色**：小胖 · 小白 · 大师

---

**小胖**：数字越大越优先还是越小越优先？

**小白**：优先级相同怎么办？FIFO 吗？

**大师**：优先级像 **「登机白金卡」**——**数值越大越优先**（以 Quartz 文档为准；若团队记忆易混，建议 **封装枚举 `Priority.HIGH`**）。同优先级时，**以触发器自然顺序 / 获取顺序** 为 tie-breaker（细节读 `QuartzSchedulerThread` 与版本实现）。

**技术映射**：**`Trigger.getPriority()`**。

---

**小胖**：线程池忙不过来，优先级还能插队吗？

**小白**：这和操作系统线程优先级是一回事吗？

**大师**：这是 **Quartz 调度层** 对 **即将被提交的 Runnable Job** 的排序提示，**不等于 `Thread.setPriority`**。就像 **机场地勤决定先推哪架飞机出库**，真正起飞还受 **跑道、空管**（线程池、GC、锁）影响。

**技术映射**：**调度层 vs OS 线程优先级**。

---

**小胖**：除了调 priority，还能怎么缓解报表饿？

**小白**：拆 Scheduler？调线程？削峰？

**大师**：三板斧：**扩容线程**、**降低低价值 Trigger 频率**、**拆分 Scheduler 实例隔离故障域**；再往上是 **架构层**：重任务 **外移 MQ** 或 **批处理平台**。

**技术映射**：**调度 + 资源 + 架构** 联合治理。

---

**小胖**：这跟食堂打饭有啥关系？我就想把任务跑起来。

**小白**：那 **谁来背锅**：触发没发生、发生了两次、还是延迟太久？指标口径先定死。

**大师**：把 **Scheduler 当「编排台」**：Job 是工序，Trigger 是节拍，Listener 是质检；节拍错了，工序再快也白搭。

**技术映射**：**可观测性口径 + Job／Trigger 职责边界**。

---

**小胖**：配置一多我就晕，`quartz.properties` 到底哪些能碰？

**小白**：**线程数、misfireThreshold、JobStore 类型** 改了会不会让 **同一套代码** 在预发与生产行为不一致？

**大师**：做一张 **「配置变更矩阵」**：改一项就写清 **影响面、回滚方式、验证命令**；RAM 与 JDBC 不要混着试。

**技术映射**：**显式配置治理 + 环境一致性**。

---

**小胖**：我本地跑得飞起，一上集群就「偶尔不跑」。

**小白**：**时钟漂移、数据库时间、JVM 默认时区** 三者不一致时，**nextFireTime** 你怎么解释给业务？

**大师**：把 **时区写进契约**：服务器、Cron、业务日历 **同一基准**；日志里同时打 **UTC 与业务时区**。

**技术映射**：**时区／DST 与触发语义**。

---

**小胖**：Trigger 优先级是不是数字越大越牛？

**小白**：**饥饿**怎么办？低优先级永远等不到的话，SLA 谁负责？

**大师**：优先级是 **「同窗口抢锁」** 的 tie-breaker，不是万能插队票；该 **拆分队列** 的别硬挤一个 Scheduler。

**技术映射**：**Trigger 优先级与吞吐隔离**。

---

**小胖**：misfire 不就是晚了吗，晚跑一下不行？

**小白**：**合并、丢弃、立即补偿** 三种策略对 **资金类任务** 分别是啥后果？

**大师**：把 **业务幂等键** 与 **misfireInstruction** 绑在一起评审；没有幂等就别选「立刻全部补上」。

**技术映射**：**misfire 策略与业务一致性**。

---

**小胖**：`JobDataMap` 里塞个大 JSON 爽不爽？

**小白**：**序列化成本、版本升级、跨语言** 谁来买单？失败重试会不会把 **半截状态** 写回去？

**大师**：**小键值 + 外置大对象**；必须进 Map 的，**版本字段** 与 **兼容读** 写进规范。

**技术映射**：**JobDataMap 体积与演进策略**。

---

**小胖**：`@DisallowConcurrentExecution` 一贴我就安心了。

**小白**：**同 JobKey 串行** 会不会把 **补偿触发** 堵成长队？线程池够吗？

**大师**：先画 **并发模型草图**：哪些 Job 必须串行、哪些只是 **资源互斥**（应改用锁或分片）。

**技术映射**：**并发注解与队列时延**。

---

**小胖**：关机我直接拔电源，反正有下次触发。

**小白**：**在途 Job** 写了一半的外部副作用怎么算？**at-least-once** 下会不会双写？

**大师**：发布路径默认 **`shutdown(true)` + 超时**；`kill -9` 只能进 **混沌演练**，不进 **常规 Runbook**。

**技术映射**：**优雅停机与副作用幂等**。

---

**小胖**：Listener 里写业务逻辑最快了。

**小白**：Listener 异常会不会 **吞掉主流程** 或 **拖慢线程**？顺序保证吗？

**大师**：Listener 只做 **旁路观测与轻量编排**；重逻辑回 **Job** 或 **下游消息**。

**技术映射**：**Listener 边界与失败隔离**。

---

**小胖**：JDBC JobStore 不就是多几张表吗？

**小白**：**行锁、delegate、方言、索引** 哪个没对齐会出现 **幽灵触发** 或 **长时间抢锁**？

**大师**：把 **DB 监控**（慢查询、锁等待）与 **Quartz 线程栈** 对齐看；调参前先 **确认隔离级别与连接池**。

**技术映射**：**持久化 JobStore 与数据库协同**。

---

**小胖**：集群一开我就加节点，TPS 一定涨吧？

**小白**：**抢锁成本、心跳、instanceId** 乱配时，会不会 **越加越慢**？

**大师**：用 **压测曲线** 证明拐点；集群收益来自 **HA 与横向扩展边界**，不是魔法按钮。

**技术映射**：**集群伸缩与锁竞争**。

---

**小胖**：我想自定义 ThreadPool 秀一把。

**小白**：线程工厂、拒绝策略、上下文传递（MDC）**漏一项** 会出现啥线上症状？

**大师**：自定义可以，但要 **对齐 SPI 契约**与 **关闭语义**；否则 **泄漏线程** 比默认池更难查。

**技术映射**：**ThreadPool SPI 与生命周期**。
## 3 项目实战（约 1500–2000 字）

### 环境准备

阅读 [PriorityExample.java](../../examples/src/main/java/org/quartz/examples/example14/PriorityExample.java) 与 [TriggerEchoJob.java](../../examples/src/main/java/org/quartz/examples/example14/TriggerEchoJob.java)。

### 分步实现

**步骤 1：目标** —— 注册两个 **同一时刻触发**、不同优先级的 Trigger，绑定不同 Job 或同一 Job（按 example 实际）。

**步骤 2：目标** —— 将 `threadCount` 设为 `1`，放大饥饿现象，再提高 **高优 Trigger** 的 `priority` 对比日志顺序。

**步骤 3：目标** —— 记录 **低优任务 misfire 计数** 随 `threadCount` 变化曲线。

### 可能踩坑

| 坑 | 解决 |
| --- | --- |
| 优先级与业务 SLA 不一致 | 文档化 |
| 集群假设全局顺序 | 每实例独立 |
| 仅调 priority 不扩容 | 治标不治本 |

### 完整代码清单

- [PriorityExample.java](../../examples/src/main/java/org/quartz/examples/example14/PriorityExample.java)

### 测试验证

并发测试：固定墙钟时间批量到期，采样 **实际执行顺序** 与 priority 相关性。

## 4 项目总结（约 500–800 字）

### 优点与缺点（对比同类技术）

| 维度 | Trigger priority | 单队列优先级 | OS nice |
| --- | --- | --- | --- |
| 语义范围 | Scheduler 内 | 队列内 | 进程级 |
| 可控性 | 中 | 高 | 低 |

### 适用 / 不适用场景

- **适用**：同池混跑多类 SLA 任务。
- **不适用**：跨系统全局优先级（需 MQ）。

### 注意事项

- **文档默认值**：升级核对。
- **可观测**：执行日志带 `triggerKey+priority`。

### 常见踩坑（生产案例）

1. **全员 HIGH**：根因是规范缺失。
2. **报表月结饿死**：根因是无低优退让窗口。
3. **误信跨节点顺序**：根因是架构误解。

#### 第20章思考题揭底

1. **协作式中断为何不能保证毫秒级**  
   **答**：Job 可能 **阻塞在不可中断 IO / native / 无检查点的 CPU 密集循环**；`interrupt` 只是 **设标志**，需代码 **主动检查与退出**；JVM 也不保证 **立即剥夺线程**。

2. **`JobInterruptMonitorPlugin` 痛点**  
   **答**：**无人值守时发现「中断请求长时间未被 Job 响应」**，提供 **监控/告警/策略钩子**（以具体实现为准），避免 **运维以为已停、实际仍在跑** 的「僵尸作业」。

### 思考题（答案见下一章或 [答案索引](answers-index.md)）

1. 高优先级 Trigger 长期占用线程时，低优先级可能出现什么？
2. 除 priority 外，还有什么手段缓解饥饿？

### 推广计划提示

- **测试**：饥饿与恢复回归。
- **运维**：SLA 看板按 priority 分面。
- **开发**：下一章进入 JDBC JobStore 与表结构。
