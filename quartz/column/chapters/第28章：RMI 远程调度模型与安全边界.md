# 第28章：RMI 远程调度模型与安全边界

> **篇别**：中级篇  
> **建议篇幅**：3000–5000 字（含对话与代码）  
> **结构约束**：对齐 [专栏模板](../template.md) 四段式。

## 示例锚点

| 类型 | 路径 |
| --- | --- |
| example12 | [RemoteServerExample.java](../../examples/src/main/java/org/quartz/examples/example12/RemoteServerExample.java) |
| example12 | [RemoteClientExample.java](../../examples/src/main/java/org/quartz/examples/example12/RemoteClientExample.java) |

## 1 项目背景（约 500 字）

### 业务场景

早期运维习惯 **在桌面工具通过 RMI 暂停/恢复远程 Scheduler**。如今企业 **零信任网络** 下，RMI **暴露对象远程调用面**、**穿越防火墙困难**、与 **Java 序列化** 风险叠加。本章通过 example12 理解 **历史模型**，并给出 **现代化替代：HTTP/gRPC + 认证鉴权 + 审计**。

### 痛点放大

- **未鉴权 RMI**：等同 **远程代码执行面**（视版本与配置）。
- **NAT/K8s**：RMI .stub 地址不可达。
- **序列化攻击面**：历史漏洞教训。

## 2 项目设计（约 1200 字）

**角色**：小胖 · 小白 · 大师

---

**小胖**：我把 `rmi.export=true` 打开就能远程管 Quartz，多方便。

**小白**：内网就能随便连吗？需要证书吗？

**大师**：RMI 像 **「在后门插一把万能钥匙」**——方便宿管，也方便小偷。生产必须 **默认 false**，若确需远程控制，应 **TLS + 强认证 + IP 白名单 + 最小权限 API**。

**技术映射**：**`org.quartz.scheduler.rmi.export`**。

---

**小胖**：那现在主流怎么做？

**小白**：Spring Boot Actuator 能替代吗？

**大师**：常见模式是 **应用内嵌管理 API**：`/admin/quartz/pause` 一类，经 **OAuth2/mTLS** 保护，操作落 **审计日志**；调度核心仍在进程内。更重的场景用 **控制面服务 + 事件总线**。

**技术映射**：**控制面与数据面分离**。

---

**小胖**：example12 我还能跑起来学习吗？

**小白**：本地如何防误开启？

**大师**：在 **隔离 VM / 容器网络命名空间** 中学习；**禁止** 将 example 默认配置复制到生产；README 标注 **「仅教学」**。

**技术映射**：**网络隔离 + 配置治理**。

---

**小胖**：这跟食堂打饭有啥关系？我就想把任务跑起来。

**小白**：那 **谁来背锅**：触发没发生、发生了两次、还是延迟太久？指标口径先定死。

**大师**：把 **Scheduler 当「编排台」**：Job 是工序，Trigger 是节拍，Listener 是质检；节拍错了，工序再快也白搭。

**技术映射**：**可观测性口径 + Job／Trigger 职责边界**。

---

**小胖**：配置一多我就晕，`quartz.properties` 到底哪些能碰？

**小白**：**线程数、misfireThreshold、JobStore 类型** 改了会不会让 **同一套代码** 在预发与生产行为不一致？

**大师**：做一张 **「配置变更矩阵」**：改一项就写清 **影响面、回滚方式、验证命令**；RAM 与 JDBC 不要混着试。

**技术映射**：**显式配置治理 + 环境一致性**。

---

**小胖**：我本地跑得飞起，一上集群就「偶尔不跑」。

**小白**：**时钟漂移、数据库时间、JVM 默认时区** 三者不一致时，**nextFireTime** 你怎么解释给业务？

**大师**：把 **时区写进契约**：服务器、Cron、业务日历 **同一基准**；日志里同时打 **UTC 与业务时区**。

**技术映射**：**时区／DST 与触发语义**。

---

**小胖**：Trigger 优先级是不是数字越大越牛？

**小白**：**饥饿**怎么办？低优先级永远等不到的话，SLA 谁负责？

**大师**：优先级是 **「同窗口抢锁」** 的 tie-breaker，不是万能插队票；该 **拆分队列** 的别硬挤一个 Scheduler。

**技术映射**：**Trigger 优先级与吞吐隔离**。

---

**小胖**：misfire 不就是晚了吗，晚跑一下不行？

**小白**：**合并、丢弃、立即补偿** 三种策略对 **资金类任务** 分别是啥后果？

**大师**：把 **业务幂等键** 与 **misfireInstruction** 绑在一起评审；没有幂等就别选「立刻全部补上」。

**技术映射**：**misfire 策略与业务一致性**。

---

**小胖**：`JobDataMap` 里塞个大 JSON 爽不爽？

**小白**：**序列化成本、版本升级、跨语言** 谁来买单？失败重试会不会把 **半截状态** 写回去？

**大师**：**小键值 + 外置大对象**；必须进 Map 的，**版本字段** 与 **兼容读** 写进规范。

**技术映射**：**JobDataMap 体积与演进策略**。

---

**小胖**：`@DisallowConcurrentExecution` 一贴我就安心了。

**小白**：**同 JobKey 串行** 会不会把 **补偿触发** 堵成长队？线程池够吗？

**大师**：先画 **并发模型草图**：哪些 Job 必须串行、哪些只是 **资源互斥**（应改用锁或分片）。

**技术映射**：**并发注解与队列时延**。

---

**小胖**：关机我直接拔电源，反正有下次触发。

**小白**：**在途 Job** 写了一半的外部副作用怎么算？**at-least-once** 下会不会双写？

**大师**：发布路径默认 **`shutdown(true)` + 超时**；`kill -9` 只能进 **混沌演练**，不进 **常规 Runbook**。

**技术映射**：**优雅停机与副作用幂等**。

---

**小胖**：Listener 里写业务逻辑最快了。

**小白**：Listener 异常会不会 **吞掉主流程** 或 **拖慢线程**？顺序保证吗？

**大师**：Listener 只做 **旁路观测与轻量编排**；重逻辑回 **Job** 或 **下游消息**。

**技术映射**：**Listener 边界与失败隔离**。

---

**小胖**：JDBC JobStore 不就是多几张表吗？

**小白**：**行锁、delegate、方言、索引** 哪个没对齐会出现 **幽灵触发** 或 **长时间抢锁**？

**大师**：把 **DB 监控**（慢查询、锁等待）与 **Quartz 线程栈** 对齐看；调参前先 **确认隔离级别与连接池**。

**技术映射**：**持久化 JobStore 与数据库协同**。

---

**小胖**：集群一开我就加节点，TPS 一定涨吧？

**小白**：**抢锁成本、心跳、instanceId** 乱配时，会不会 **越加越慢**？

**大师**：用 **压测曲线** 证明拐点；集群收益来自 **HA 与横向扩展边界**，不是魔法按钮。

**技术映射**：**集群伸缩与锁竞争**。

---

**小胖**：我想自定义 ThreadPool 秀一把。

**小白**：线程工厂、拒绝策略、上下文传递（MDC）**漏一项** 会出现啥线上症状？

**大师**：自定义可以，但要 **对齐 SPI 契约**与 **关闭语义**；否则 **泄漏线程** 比默认池更难查。

**技术映射**：**ThreadPool SPI 与生命周期**。
## 3 项目实战（约 1500–2000 字）

### 环境准备

阅读 [RemoteServerExample.java](../../examples/src/main/java/org/quartz/examples/example12/RemoteServerExample.java) 与 Client，理解 **Registry 绑定流程**。

### 分步实现

**步骤 1：目标** —— 本地启动 Server，再启动 Client，观察 **远程 `scheduleJob` 是否生效**（严格按 example README/注释操作）。

**步骤 2：目标** —— 用 **tcpdump/wireshark** 观察 RMI 端口流量（实验环境）。

**步骤 3：目标** —— 设计替代：**REST pause/resume** 伪代码 + **JWT**。

### 可能踩坑

| 坑 | 解决 |
| --- | --- |
| 防火墙 | 不走 RMI |
| 类版本不一致 | 统一 classpath |
| 序列化白名单 | JDK 配置 |

### 完整代码清单

- [RemoteServerExample.java](../../examples/src/main/java/org/quartz/examples/example12/RemoteServerExample.java)

### 测试验证

安全扫描：依赖 CVE 检查；渗透测试（若企业要求）。

## 4 项目总结（约 500–800 字）

### 优点与缺点（对比同类技术）

| 维度 | RMI | HTTPS API |
| --- | --- | --- |
| 生态 | 老 | 新 |
| 安全治理 | 难 | 易 |

### 适用 / 不适用场景

- **适用**：维护遗留、内网封闭且已加固。
- **不适用**：互联网暴露、K8s 默认网络。

### 注意事项

- **默认关闭 RMI**。
- **合规**：等保测评项。

### 常见踩坑（生产案例）

1. **公网暴露 RMI**：根因是配置复制。
2. **无审计**：根因是运维随意 pause。
3. **stub 地址错误**：根因是 NAT。

#### 第27章思考题揭底

1. **`batchTriggerAcquisitionMaxCount` 调大**  
   **答**：**减少 `acquire` SQL 次数、降低 DB round-trip**，但单次事务 **持有锁时间变长**、**可能拉高 p99 延迟**，并在 **冲突时放大重试成本**。需 **压测** 找到甜点区。

2. **`LoadExample` 启示**  
   **答**：揭示 **高并发触发 + 短 Job** 下 **线程池、批量拉取、misfire、DB 热点** 的联动关系；生产应建立 **与真实曲线相近的压测模型**，而非只盯 CPU。

### 思考题（答案见下一章或 [答案索引](answers-index.md)）

1. RMI 暴露 Scheduler 的主要安全风险是什么？
2. 若必须远程控制，更现代的替代架构是什么？

### 推广计划提示

- **测试**：安全基线扫描。
- **运维**：网络策略默认拒绝。
- **开发**：下一章为 **中级篇综合实战**（独立章节），随后进入 `StdSchedulerFactory` 源码导读。
