# 第32章：`RAMJobStore`：内存结构、触发顺序

> **篇别**：高级篇  
> **建议篇幅**：3000–5000 字（含对话与代码）  
> **结构约束**：对齐 [专栏模板](../template.md) 四段式。

## 示例锚点

| 类型 | 路径 |
| --- | --- |
| 源码 | [RAMJobStore.java](../../quartz/src/main/java/org/quartz/simpl/RAMJobStore.java) |

## 1 项目背景（约 500 字）

### 业务场景

压测单机 **纯内存调度** 上限，为 **「是否需要上 JDBC」** 提供数据。团队发现 RAM 模式下 **CPU 不高但延迟上升**，怀疑 **`TreeSet`/`HashMap` 锁竞争** 或 **`acquireNextTriggers` 排序成本**。本章引导阅读 **`RAMJobStore` 的触发器存储与加锁粒度**。

### 痛点放大

- **误以为 RAM 无锁**：仍有 **并发控制**。
- **大触发器集合**：内存与比较器成本。
- **与 JDBC 行为差异**：misfire、集群语义不同。

## 2 项目设计（约 1200 字）

**角色**：小胖 · 小白 · 大师

---

**小胖**：RAM 就是 HashMap 吧？为啥还复杂？

**小白**：触发排序用啥数据结构？为什么不用 ArrayList 每次 sort？

**大师**：需要 **按 nextFireTime 快速取最小堆/有序结构** 的思想（具体结构读源码）。像 **「取下一个叫号的病人」**——不能每次全表排序。

**技术映射**：**`acquireNextTriggers` 的排序与过滤**。

---

**小胖**：RAM 的 misfire 和 JDBC 一样吗？

**小白**：阈值相同情况下行为差异点在哪？

**大师**：**判定逻辑** 共享大量 Trigger 代码路径，但 **持久化与集群锁** 不存在；因此 **「恢复与可见性」** 不同。RAM 进程死即 **全丢**。

**技术映射**：**进程内一致性 vs 跨进程持久化**。

---

**小胖**：我们能在 RAM 模式压到每秒一万触发吗？

**小白**：瓶颈更像 CPU 还是锁？

**大师**：要 **profile**：常见瓶颈在 **锁竞争 + Job 本身执行**；一万空触发未必等价一万业务 Job。

**技术映射**：**JFR/async-profiler**。

---

**小胖**：这跟食堂打饭有啥关系？我就想把任务跑起来。

**小白**：那 **谁来背锅**：触发没发生、发生了两次、还是延迟太久？指标口径先定死。

**大师**：把 **Scheduler 当「编排台」**：Job 是工序，Trigger 是节拍，Listener 是质检；节拍错了，工序再快也白搭。

**技术映射**：**可观测性口径 + Job／Trigger 职责边界**。

---

**小胖**：配置一多我就晕，`quartz.properties` 到底哪些能碰？

**小白**：**线程数、misfireThreshold、JobStore 类型** 改了会不会让 **同一套代码** 在预发与生产行为不一致？

**大师**：做一张 **「配置变更矩阵」**：改一项就写清 **影响面、回滚方式、验证命令**；RAM 与 JDBC 不要混着试。

**技术映射**：**显式配置治理 + 环境一致性**。

---

**小胖**：我本地跑得飞起，一上集群就「偶尔不跑」。

**小白**：**时钟漂移、数据库时间、JVM 默认时区** 三者不一致时，**nextFireTime** 你怎么解释给业务？

**大师**：把 **时区写进契约**：服务器、Cron、业务日历 **同一基准**；日志里同时打 **UTC 与业务时区**。

**技术映射**：**时区／DST 与触发语义**。

---

**小胖**：Trigger 优先级是不是数字越大越牛？

**小白**：**饥饿**怎么办？低优先级永远等不到的话，SLA 谁负责？

**大师**：优先级是 **「同窗口抢锁」** 的 tie-breaker，不是万能插队票；该 **拆分队列** 的别硬挤一个 Scheduler。

**技术映射**：**Trigger 优先级与吞吐隔离**。

---

**小胖**：misfire 不就是晚了吗，晚跑一下不行？

**小白**：**合并、丢弃、立即补偿** 三种策略对 **资金类任务** 分别是啥后果？

**大师**：把 **业务幂等键** 与 **misfireInstruction** 绑在一起评审；没有幂等就别选「立刻全部补上」。

**技术映射**：**misfire 策略与业务一致性**。

---

**小胖**：`JobDataMap` 里塞个大 JSON 爽不爽？

**小白**：**序列化成本、版本升级、跨语言** 谁来买单？失败重试会不会把 **半截状态** 写回去？

**大师**：**小键值 + 外置大对象**；必须进 Map 的，**版本字段** 与 **兼容读** 写进规范。

**技术映射**：**JobDataMap 体积与演进策略**。

---

**小胖**：`@DisallowConcurrentExecution` 一贴我就安心了。

**小白**：**同 JobKey 串行** 会不会把 **补偿触发** 堵成长队？线程池够吗？

**大师**：先画 **并发模型草图**：哪些 Job 必须串行、哪些只是 **资源互斥**（应改用锁或分片）。

**技术映射**：**并发注解与队列时延**。

---

**小胖**：关机我直接拔电源，反正有下次触发。

**小白**：**在途 Job** 写了一半的外部副作用怎么算？**at-least-once** 下会不会双写？

**大师**：发布路径默认 **`shutdown(true)` + 超时**；`kill -9` 只能进 **混沌演练**，不进 **常规 Runbook**。

**技术映射**：**优雅停机与副作用幂等**。

---

**小胖**：Listener 里写业务逻辑最快了。

**小白**：Listener 异常会不会 **吞掉主流程** 或 **拖慢线程**？顺序保证吗？

**大师**：Listener 只做 **旁路观测与轻量编排**；重逻辑回 **Job** 或 **下游消息**。

**技术映射**：**Listener 边界与失败隔离**。

---

**小胖**：JDBC JobStore 不就是多几张表吗？

**小白**：**行锁、delegate、方言、索引** 哪个没对齐会出现 **幽灵触发** 或 **长时间抢锁**？

**大师**：把 **DB 监控**（慢查询、锁等待）与 **Quartz 线程栈** 对齐看；调参前先 **确认隔离级别与连接池**。

**技术映射**：**持久化 JobStore 与数据库协同**。

---

**小胖**：集群一开我就加节点，TPS 一定涨吧？

**小白**：**抢锁成本、心跳、instanceId** 乱配时，会不会 **越加越慢**？

**大师**：用 **压测曲线** 证明拐点；集群收益来自 **HA 与横向扩展边界**，不是魔法按钮。

**技术映射**：**集群伸缩与锁竞争**。

---

**小胖**：我想自定义 ThreadPool 秀一把。

**小白**：线程工厂、拒绝策略、上下文传递（MDC）**漏一项** 会出现啥线上症状？

**大师**：自定义可以，但要 **对齐 SPI 契约**与 **关闭语义**；否则 **泄漏线程** 比默认池更难查。

**技术映射**：**ThreadPool SPI 与生命周期**。
## 3 项目实战（约 1500–2000 字）

### 环境准备

克隆源码，打开 `RAMJobStore`，搜索 **`acquireNextTriggers`**。

### 分步实现

**步骤 1：目标** —— 本地写 micro-benchmark：**10w Trigger** 注册（谨慎内存）观察耗时（实验性质）。

**步骤 2：目标** —— 切换 **同配置 JDBC**，对比 **schedule 延迟**。

**步骤 3：目标** —— `jstack` 查看 **锁热点**。

### 可能踩坑

| 坑 | 解决 |
| --- | --- |
| OOM | 减少实验规模 |
| 混测业务 Job | 分离变量 |
| 误用 profiler 采样偏差 | 多种工具交叉 |

### 完整代码清单

- [RAMJobStore.java](../../quartz/src/main/java/org/quartz/simpl/RAMJobStore.java)

### 测试验证

单元测试：验证 **同 nextFireTime** 时 **优先级 tie-break**（若适用）。

## 4 项目总结（约 500–800 字）

### 优点与缺点（对比同类技术）

| 维度 | RAMJobStore | JDBCJobStore |
| --- | --- | --- |
| 吞吐潜力 | 高 | 受 DB 限 |
| 持久化 | 无 | 有 |

### 适用 / 不适用场景

- **适用**：测试、原型、可丢任务。
- **不适用**：生产关键路径（除非接受丢）。

### 注意事项

- **Heap**：Trigger 数量与 JobDataMap 大小。
- **GC**：频繁构造对象。

### 常见踩坑（生产案例）

1. **误把 RAM 上生产**：根因是配置漂移。
2. **内存膨胀**：根因是 JobDataMap 存大对象。
3. **调试环境与生产不一致**：根因是无配置基线。

#### 第31章思考题揭底

1. **时间推进与拉取协作**  
   **答**：调度线程 **根据当前时间与触发器时间线** 调用 **`JobStore.acquireNextTriggers`** 获取 **即将到期的一批触发器**；再交给 **线程池执行**；执行结果回调 **`JobStore` 更新状态** 并 **计算下一次 fire time**；`signaler` 在状态变更时 **唤醒等待**。整体是 **拉取-执行-反馈** 闭环。

2. **阻塞与唤醒条件**  
   **答**：无可用触发器时 **等待至最近 nextFire** 或被 **`signaler.signal`**（如 **新任务 schedule、pause/resume**）唤醒；具体 **`wait`/`await`** 以 `QuartzSchedulerThread` 源码为准。

### 思考题（答案见下一章或 [答案索引](answers-index.md)）

1. RAMJobStore 下 misfire 行为与 JDBC 是否一致？
2. RAMJobStore 的瓶颈通常在 CPU 还是锁？

### 推广计划提示

- **测试**：内存与延迟基准。
- **运维**：禁止 RAM 用于关键任务（治理）。
- **开发**：下一章 `JobStoreSupport` 锁与行锁。
