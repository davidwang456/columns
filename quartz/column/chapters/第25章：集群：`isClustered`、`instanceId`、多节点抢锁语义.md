# 第25章：集群：`isClustered`、`instanceId`、多节点抢锁语义

> **篇别**：中级篇  
> **建议篇幅**：3000–5000 字（含对话与代码）  
> **结构约束**：对齐 [专栏模板](../template.md) 四段式。

## 示例锚点

| 类型 | 路径 |
| --- | --- |
| example13 | [ClusterExample.java](../../examples/src/main/java/org/quartz/examples/example13/ClusterExample.java) |

## 1 项目背景（约 500 字）

### 业务场景

双活应用部署在 **两个 K8s Pod**，希望 **同一业务 Job 任一时刻只在一个节点执行**。启用 **`JobStoreTX` + `org.quartz.jobStore.isClustered=true`** 后，仍出现 **「偶发双跑」** 或 **「节点掉线后长时间无人接手」**——多与 **`instanceId` 冲突、时钟漂移、数据库锁等待** 有关。本章结合 example13 建立 **集群心智模型**。

### 痛点放大

- **`instanceId` 自动生成碰撞**（概率低但可配置）。
- **时钟不同步**：触发时间比较异常。
- **误用 RAMJobStore 集群**：根本不协调。

## 2 项目设计（约 1200 字）

**角色**：小胖 · 小白 · 大师

---

**小胖**：集群不就是两个应用一起跑吗？为啥还要数据库？

**小白**：抢锁会不会把数据库打爆？

**大师**：可以把 **JDBC 集群** 想成 **「多个调度员共看一块白板，只能一个人上去改下一班岗」**——数据库提供 **谁抢到锁谁更新 `FIRED_TRIGGERS`」** 的场地。没有 DB，就像 **两人各拿一块自己的白板**，必然 **双跑或漏跑**。

**技术映射**：**`isClustered` + 行锁/信号量**（第31章）。

---

**小胖**：`instanceId` 我全用 `AUTO` 行不行？

**小白**：Pod 重建后 id 变不变？会不会把自己当陌生人？

**大师**：`AUTO` 通常生成 **唯一 id**；Pod 频繁重建会导致 **「实例 churn」**——一般无害，但 **日志追踪** 变复杂。可结合 **hostname + uuid** 策略（见 `HostnameInstanceIdGenerator` 等扩展，第33章）。

**技术映射**：**`org.quartz.scheduler.instanceId`**。

---

**小胖**：同一任务执行两次，用户会骂街，怎么防？

**小白**：幂等之外，调度层还能做什么？

**大师**：调度层保证 **「同一触发器记录不会被两节点同时认领」**；业务仍要 **幂等**，因为 **崩溃发生在 `execute` 中途** 时，**at-least-once** 仍可能发生。

**技术映射**：**集群 = 互斥领取 + 业务幂等**。

---

**小胖**：这跟食堂打饭有啥关系？我就想把任务跑起来。

**小白**：那 **谁来背锅**：触发没发生、发生了两次、还是延迟太久？指标口径先定死。

**大师**：把 **Scheduler 当「编排台」**：Job 是工序，Trigger 是节拍，Listener 是质检；节拍错了，工序再快也白搭。

**技术映射**：**可观测性口径 + Job／Trigger 职责边界**。

---

**小胖**：配置一多我就晕，`quartz.properties` 到底哪些能碰？

**小白**：**线程数、misfireThreshold、JobStore 类型** 改了会不会让 **同一套代码** 在预发与生产行为不一致？

**大师**：做一张 **「配置变更矩阵」**：改一项就写清 **影响面、回滚方式、验证命令**；RAM 与 JDBC 不要混着试。

**技术映射**：**显式配置治理 + 环境一致性**。

---

**小胖**：我本地跑得飞起，一上集群就「偶尔不跑」。

**小白**：**时钟漂移、数据库时间、JVM 默认时区** 三者不一致时，**nextFireTime** 你怎么解释给业务？

**大师**：把 **时区写进契约**：服务器、Cron、业务日历 **同一基准**；日志里同时打 **UTC 与业务时区**。

**技术映射**：**时区／DST 与触发语义**。

---

**小胖**：Trigger 优先级是不是数字越大越牛？

**小白**：**饥饿**怎么办？低优先级永远等不到的话，SLA 谁负责？

**大师**：优先级是 **「同窗口抢锁」** 的 tie-breaker，不是万能插队票；该 **拆分队列** 的别硬挤一个 Scheduler。

**技术映射**：**Trigger 优先级与吞吐隔离**。

---

**小胖**：misfire 不就是晚了吗，晚跑一下不行？

**小白**：**合并、丢弃、立即补偿** 三种策略对 **资金类任务** 分别是啥后果？

**大师**：把 **业务幂等键** 与 **misfireInstruction** 绑在一起评审；没有幂等就别选「立刻全部补上」。

**技术映射**：**misfire 策略与业务一致性**。

---

**小胖**：`JobDataMap` 里塞个大 JSON 爽不爽？

**小白**：**序列化成本、版本升级、跨语言** 谁来买单？失败重试会不会把 **半截状态** 写回去？

**大师**：**小键值 + 外置大对象**；必须进 Map 的，**版本字段** 与 **兼容读** 写进规范。

**技术映射**：**JobDataMap 体积与演进策略**。

---

**小胖**：`@DisallowConcurrentExecution` 一贴我就安心了。

**小白**：**同 JobKey 串行** 会不会把 **补偿触发** 堵成长队？线程池够吗？

**大师**：先画 **并发模型草图**：哪些 Job 必须串行、哪些只是 **资源互斥**（应改用锁或分片）。

**技术映射**：**并发注解与队列时延**。

---

**小胖**：关机我直接拔电源，反正有下次触发。

**小白**：**在途 Job** 写了一半的外部副作用怎么算？**at-least-once** 下会不会双写？

**大师**：发布路径默认 **`shutdown(true)` + 超时**；`kill -9` 只能进 **混沌演练**，不进 **常规 Runbook**。

**技术映射**：**优雅停机与副作用幂等**。

---

**小胖**：Listener 里写业务逻辑最快了。

**小白**：Listener 异常会不会 **吞掉主流程** 或 **拖慢线程**？顺序保证吗？

**大师**：Listener 只做 **旁路观测与轻量编排**；重逻辑回 **Job** 或 **下游消息**。

**技术映射**：**Listener 边界与失败隔离**。

---

**小胖**：JDBC JobStore 不就是多几张表吗？

**小白**：**行锁、delegate、方言、索引** 哪个没对齐会出现 **幽灵触发** 或 **长时间抢锁**？

**大师**：把 **DB 监控**（慢查询、锁等待）与 **Quartz 线程栈** 对齐看；调参前先 **确认隔离级别与连接池**。

**技术映射**：**持久化 JobStore 与数据库协同**。

---

**小胖**：集群一开我就加节点，TPS 一定涨吧？

**小白**：**抢锁成本、心跳、instanceId** 乱配时，会不会 **越加越慢**？

**大师**：用 **压测曲线** 证明拐点；集群收益来自 **HA 与横向扩展边界**，不是魔法按钮。

**技术映射**：**集群伸缩与锁竞争**。

---

**小胖**：我想自定义 ThreadPool 秀一把。

**小白**：线程工厂、拒绝策略、上下文传递（MDC）**漏一项** 会出现啥线上症状？

**大师**：自定义可以，但要 **对齐 SPI 契约**与 **关闭语义**；否则 **泄漏线程** 比默认池更难查。

**技术映射**：**ThreadPool SPI 与生命周期**。
## 3 项目实战（约 1500–2000 字）

### 环境准备

阅读 [ClusterExample.java](../../examples/src/main/java/org/quartz/examples/example13/ClusterExample.java) 与随附 **properties**（若 example 目录内有）。

### 分步实现

**步骤 1：目标** —— 配置 `isClustered=true`、`instanceId=AUTO`。

**步骤 2：目标** —— 启动 **两个 JVM 进程** 指向 **同一数据库**，观察日志 **只有一个节点执行同一 Trigger**。

**步骤 3：目标** —— 人为 **拔网线/杀 Pod** 模拟故障，观察 **另一节点接管延迟**。

### 可能踩坑

| 坑 | 解决 |
| --- | --- |
| 未用 JDBC JobStore | 无法集群 |
| 时间不同步 | NTP |
| 短租约+长任务 | 调整阈值与中断策略 |

### 完整代码清单

- [ClusterExample.java](../../examples/src/main/java/org/quartz/examples/example13/ClusterExample.java)

### 测试验证

混沌：随机 kill 节点，断言 **业务副作用次数上界**。

## 4 项目总结（约 500–800 字）

### 优点与缺点（对比同类技术）

| 维度 | Quartz JDBC 集群 | 单节点+分布式锁 | K8s leader election |
| --- | --- | --- | --- |
| 与调度集成 | 高 | 中 | 中 |
| DB 依赖 | 是 | 是 | 否 |

### 适用 / 不适用场景

- **适用**：Java 进程横向扩展、已有 RDBMS。
- **不适用**：无共享存储、强边缘自治。

### 注意事项

- **DB 高可用**：主从切换窗口。
- **防火墙**：实例间 **可选** 直连需求（RMI 另论，第27章）。

### 常见踩坑（生产案例）

1. **双主 DB 导致脑裂**：根因是复制模式误解。
2. **时钟漂移**：根因是 NTP 未开。
3. **instanceName 相同 instanceId 撞车**：根因是复制 VM 镜像。

#### 第24章思考题揭底

1. **为何需要 `ConnectionProvider`**  
   **答**：为 Quartz 提供 **可池化、可托管、可替换** 的取连接抽象，避免 **`DriverManager.getConnection` 每次建连** 的性能灾难，并便于 **与容器/Spring 数据源集成**。

2. **`maximumPoolSize` 与 `threadCount` 配比**  
   **经验法则**：**池大小 ≥ 并发执行可能占用的连接数 + 调度线程与 JobStore 内部查询余量**；起点可取 **`maxPool ≈ threadCount + 10~20%`**，再以 **压测与 DB max_connections** 校正；出现 **连接等待** 或 **misfire** 同时升高时应 **同步上调池或优化 SQL**。

### 思考题（答案见下一章或 [答案索引](answers-index.md)）

1. 集群模式下「同一任务会执行两次」的根因通常有哪些？
2. `instanceId` 若冲突会发生什么？

### 推广计划提示

- **测试**：双节点 soak。
- **运维**：NTP、DB 主从监控。
- **开发**：下一章 `requestsRecovery`。
