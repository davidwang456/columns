# 第31章：`QuartzScheduler` 与 `QuartzSchedulerThread` 主循环

> **篇别**：高级篇  
> **建议篇幅**：3000–5000 字（含对话与代码）  
> **结构约束**：对齐 [专栏模板](../template.md) 四段式。

## 示例锚点

| 类型 | 路径 |
| --- | --- |
| 源码 | [QuartzScheduler.java](../../quartz/src/main/java/org/quartz/core/QuartzScheduler.java) |
| 源码 | [QuartzSchedulerThread.java](../../quartz/src/main/java/org/quartz/core/QuartzSchedulerThread.java) |

## 1 项目背景（约 500 字）

### 业务场景

线上出现 **p99 触发延迟偶发飙升**，CPU 不高、DB 偶发慢查询。资深开发怀疑 **`QuartzSchedulerThread` 在 `acquireNextTriggers` 阻塞** 或 **`sigLock.wait`** 等待信号。需要 **能读懂主循环**：何时 **休眠**、何时 **被唤醒**、何时 **提交 JobRunShell 到线程池**。

### 痛点放大

- **把调度线程当业务线程**：在循环里塞重逻辑（错误）。
- **忽略 `signaler`**：pause/resume 不生效的误排。
- **thread dump 不会读**：看不到 `QuartzSchedulerThread`。

## 2 项目设计（约 1200 字）

**角色**：小胖 · 小白 · 大师

---

**小胖**：调度线程是不是 while true 一直转？

**小白**：没有任务时会空转烧 CPU 吗？

**大师**：优质循环是 **「有活干活，没活睡觉，被叫醒」**——`QuartzSchedulerThread` 在 **无触发器可运行** 时会 **等待直到 next fire time** 附近被唤醒，而不是 busy spin。

**技术映射**：**`sigLock` + `wait/signal`**（以源码为准）。

---

**小胖**：`runInThread` 谁调用谁？

**小白**：JobStore 拉取和线程池提交边界在哪？

**大师**：可把 **调度线程** 想成 **「派单员」**，**线程池** 是 **「骑手」**——派单员从 **中央仓库（JobStore）** 取下一件可配送订单（TriggerBundle），交给骑手执行；派单员 **自己不送外卖**（不做业务）。

**技术映射**：**`ThreadPool.runInThread(Runnable)`**。

---

**小胖**：主循环能加 trace 吗？

**小白**：OpenTelemetry 插桩点选哪里？

**大师**：在 **`acquireNextTriggers` 前后** 与 **`JobRunShell` 开始/结束`** 打点；避免在 **JobStore 锁内** 打重日志。

**技术映射**：**低开销观测**。

---

**小胖**：这跟食堂打饭有啥关系？我就想把任务跑起来。

**小白**：那 **谁来背锅**：触发没发生、发生了两次、还是延迟太久？指标口径先定死。

**大师**：把 **Scheduler 当「编排台」**：Job 是工序，Trigger 是节拍，Listener 是质检；节拍错了，工序再快也白搭。

**技术映射**：**可观测性口径 + Job／Trigger 职责边界**。

---

**小胖**：配置一多我就晕，`quartz.properties` 到底哪些能碰？

**小白**：**线程数、misfireThreshold、JobStore 类型** 改了会不会让 **同一套代码** 在预发与生产行为不一致？

**大师**：做一张 **「配置变更矩阵」**：改一项就写清 **影响面、回滚方式、验证命令**；RAM 与 JDBC 不要混着试。

**技术映射**：**显式配置治理 + 环境一致性**。

---

**小胖**：我本地跑得飞起，一上集群就「偶尔不跑」。

**小白**：**时钟漂移、数据库时间、JVM 默认时区** 三者不一致时，**nextFireTime** 你怎么解释给业务？

**大师**：把 **时区写进契约**：服务器、Cron、业务日历 **同一基准**；日志里同时打 **UTC 与业务时区**。

**技术映射**：**时区／DST 与触发语义**。

---

**小胖**：Trigger 优先级是不是数字越大越牛？

**小白**：**饥饿**怎么办？低优先级永远等不到的话，SLA 谁负责？

**大师**：优先级是 **「同窗口抢锁」** 的 tie-breaker，不是万能插队票；该 **拆分队列** 的别硬挤一个 Scheduler。

**技术映射**：**Trigger 优先级与吞吐隔离**。

---

**小胖**：misfire 不就是晚了吗，晚跑一下不行？

**小白**：**合并、丢弃、立即补偿** 三种策略对 **资金类任务** 分别是啥后果？

**大师**：把 **业务幂等键** 与 **misfireInstruction** 绑在一起评审；没有幂等就别选「立刻全部补上」。

**技术映射**：**misfire 策略与业务一致性**。

---

**小胖**：`JobDataMap` 里塞个大 JSON 爽不爽？

**小白**：**序列化成本、版本升级、跨语言** 谁来买单？失败重试会不会把 **半截状态** 写回去？

**大师**：**小键值 + 外置大对象**；必须进 Map 的，**版本字段** 与 **兼容读** 写进规范。

**技术映射**：**JobDataMap 体积与演进策略**。

---

**小胖**：`@DisallowConcurrentExecution` 一贴我就安心了。

**小白**：**同 JobKey 串行** 会不会把 **补偿触发** 堵成长队？线程池够吗？

**大师**：先画 **并发模型草图**：哪些 Job 必须串行、哪些只是 **资源互斥**（应改用锁或分片）。

**技术映射**：**并发注解与队列时延**。

---

**小胖**：关机我直接拔电源，反正有下次触发。

**小白**：**在途 Job** 写了一半的外部副作用怎么算？**at-least-once** 下会不会双写？

**大师**：发布路径默认 **`shutdown(true)` + 超时**；`kill -9` 只能进 **混沌演练**，不进 **常规 Runbook**。

**技术映射**：**优雅停机与副作用幂等**。

---

**小胖**：Listener 里写业务逻辑最快了。

**小白**：Listener 异常会不会 **吞掉主流程** 或 **拖慢线程**？顺序保证吗？

**大师**：Listener 只做 **旁路观测与轻量编排**；重逻辑回 **Job** 或 **下游消息**。

**技术映射**：**Listener 边界与失败隔离**。

---

**小胖**：JDBC JobStore 不就是多几张表吗？

**小白**：**行锁、delegate、方言、索引** 哪个没对齐会出现 **幽灵触发** 或 **长时间抢锁**？

**大师**：把 **DB 监控**（慢查询、锁等待）与 **Quartz 线程栈** 对齐看；调参前先 **确认隔离级别与连接池**。

**技术映射**：**持久化 JobStore 与数据库协同**。

---

**小胖**：集群一开我就加节点，TPS 一定涨吧？

**小白**：**抢锁成本、心跳、instanceId** 乱配时，会不会 **越加越慢**？

**大师**：用 **压测曲线** 证明拐点；集群收益来自 **HA 与横向扩展边界**，不是魔法按钮。

**技术映射**：**集群伸缩与锁竞争**。

---

**小胖**：我想自定义 ThreadPool 秀一把。

**小白**：线程工厂、拒绝策略、上下文传递（MDC）**漏一项** 会出现啥线上症状？

**大师**：自定义可以，但要 **对齐 SPI 契约**与 **关闭语义**；否则 **泄漏线程** 比默认池更难查。

**技术映射**：**ThreadPool SPI 与生命周期**。

---

**小胖**：这跟食堂打饭有啥关系？我就想把任务跑起来。

**小白**：那 **谁来背锅**：触发没发生、发生了两次、还是延迟太久？指标口径先定死。

**大师**：把 **Scheduler 当「编排台」**：Job 是工序，Trigger 是节拍，Listener 是质检；节拍错了，工序再快也白搭。

**技术映射**：**可观测性口径 + Job／Trigger 职责边界**。
## 3 项目实战（约 1500–2000 字）

### 环境准备

IDE 打开 `QuartzSchedulerThread#run`，配合断点 **条件触发**。

### 分步实现

**步骤 1：目标** —— 标注 **关键方法调用链**（手写笔记）：`idle` → `acquire` → `triggersFired` → `release`。

**步骤 2：目标** —— 打开 **DEBUG 日志 `org.quartz`**（注意脱敏与性能）。

**步骤 3：目标** —— 与 **JDBC 慢 SQL** 时间线对齐，确认延迟来自 **拉取** 还是 **业务 Job**。

### 可能踩坑

| 坑 | 解决 |
| --- | --- |
| 断点拖慢生产 | 仅非 prod |
| 误判循环 bug | 对比版本差异 |
| 日志风暴 | 采样 |

### 完整代码清单

- [QuartzSchedulerThread.java](../../quartz/src/main/java/org/quartz/core/QuartzSchedulerThread.java)

### 测试验证

本地注入 **人为慢 JobStore**（mock）观察调度线程行为。

## 4 项目总结（约 500–800 字）

### 优点与缺点（对比同类技术）

| 维度 | 读源码排障 | 仅看配置 |
| --- | --- | --- |
| 深度 | 高 | 低 |
| 成本 | 高 | 低 |

### 适用 / 不适用场景

- **适用**：延迟类 P1 事故。
- **不适用**：日常 CRUD 业务排障。

### 注意事项

- **许可证与版权**：IBM/Terracotta 头注释。
- **升级 diff**：主循环可能有优化。

### 常见踩坑（生产案例）

1. **把 DB 慢全怪业务**：根因是 acquire SQL。
2. **pause 无效**：根因是信号未送达。
3. **线程泄漏**：根因是 shutdown 未调用。

#### 第30章思考题揭底

1. **最关键 3 类组件**  
   **答**：**`ThreadPool`（执行 Worker）**、**`JobStore`（触发器与状态持久化/内存）**、**`QuartzScheduler` + `QuartzSchedulerThread`（时间推进与派发）**；通常再加上 **`JobRunShellFactory` / `SchedulerSignaler`** 等协作件。

2. **多份 `quartz.properties` 加载顺序**  
   **答**：取决于 **`StdSchedulerFactory` 使用的 `ClassLoader#getResource` 搜索顺序**——**先找到的优先**（与 classpath 顺序、FatJar 合并策略相关）。**生产应显式传入唯一配置文件路径**或使用 **Spring `Resource` 明确 URI**。

### 思考题（答案见下一章或 [答案索引](answers-index.md)）

1. 主循环中「时间推进」与「拉取触发器」大致如何协作？
2. `QuartzSchedulerThread` 阻塞与唤醒的主要条件？

### 推广计划提示

- **测试**：延迟注入单测。
- **运维**：线程 dump SOP。
- **开发**：下一章 `RAMJobStore` 数据结构。
