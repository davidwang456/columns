# 第33章：`JobStoreSupport`：拉取触发器、`Semaphore`、行锁

> **篇别**：高级篇  
> **建议篇幅**：3000–5000 字（含对话与代码）  
> **结构约束**：对齐 [专栏模板](../template.md) 四段式。

## 示例锚点

| 类型 | 路径 |
| --- | --- |
| 源码 | [JobStoreSupport.java](../../quartz/src/main/java/org/quartz/impl/jdbcjobstore/JobStoreSupport.java) |

## 1 项目背景（约 500 字）

### 业务场景

JDBC 集群下偶发 **`LockAcquisitionException`** 与 **DB 死锁**，DBA 抓取 SQL 显示 **`SELECT ... FOR UPDATE`** 与 **`UPDATE QRTZ_TRIGGERS`** 交错。需要理解 **`JobStoreSupport` 如何通过 `Semaphore` 与 driver delegate 协调「实例内互斥」与「数据库行锁」**，避免 **误调参数导致锁风暴**。

### 痛点放大

- **`SimpleSemaphore` vs `DBSemaphore`**：集群语义不同。
- **`clustered` 下错误 delegate**：锁 SQL 不兼容。
- **长事务 Job**：拉长锁持有。

## 2 项目设计（约 1200 字）

**角色**：小胖 · 小白 · 大师

---

**小胖**：数据库已经有事务了，还要 `Semaphore` 干嘛？

**小白**：实例内信号量和数据库行锁会不会「双锁」死锁？

**大师**：可以把 **`Semaphore` 想成「办公室门口保安」**，**行锁** 想成 **「金库门上的银行锁」**——保安防止 **同一进程里重复进金库流程**，银行锁防止 **两个分行同时搬同一箱现金**。两者解决 **不同层次的并发**；配置错误才会 **互相等待**。

**技术映射**：**`org.quartz.jobStore.lockHandler.class` / `Semaphore` 实现**（键名以文档为准）。

---

**小胖**：`StdRowLockSemaphore` 是什么？

**小白**：MySQL `GET_LOCK` 和 `FOR UPDATE` 选哪个？

**大师**：以 **delegate 与版本实现** 为准；原则是 **短事务、快速提交**、**避免与业务大事务交叉**。读 `JobStoreSupport#executeInNonManagedTXLock` 一类方法注释。

**技术映射**：**锁范围最小化**。

---

**小胖**：我们能关掉锁换性能吗？

**小白**：单节点能不能 SimpleSemaphore？

**大师**：**单节点 RAM** 可以更简单；**JDBC 集群不要作死关锁**——除非你想体验 **双跑宇宙**。

**技术映射**：**集群正确性 > 微优化**。

---

**小胖**：这跟食堂打饭有啥关系？我就想把任务跑起来。

**小白**：那 **谁来背锅**：触发没发生、发生了两次、还是延迟太久？指标口径先定死。

**大师**：把 **Scheduler 当「编排台」**：Job 是工序，Trigger 是节拍，Listener 是质检；节拍错了，工序再快也白搭。

**技术映射**：**可观测性口径 + Job／Trigger 职责边界**。

---

**小胖**：配置一多我就晕，`quartz.properties` 到底哪些能碰？

**小白**：**线程数、misfireThreshold、JobStore 类型** 改了会不会让 **同一套代码** 在预发与生产行为不一致？

**大师**：做一张 **「配置变更矩阵」**：改一项就写清 **影响面、回滚方式、验证命令**；RAM 与 JDBC 不要混着试。

**技术映射**：**显式配置治理 + 环境一致性**。

---

**小胖**：我本地跑得飞起，一上集群就「偶尔不跑」。

**小白**：**时钟漂移、数据库时间、JVM 默认时区** 三者不一致时，**nextFireTime** 你怎么解释给业务？

**大师**：把 **时区写进契约**：服务器、Cron、业务日历 **同一基准**；日志里同时打 **UTC 与业务时区**。

**技术映射**：**时区／DST 与触发语义**。

---

**小胖**：Trigger 优先级是不是数字越大越牛？

**小白**：**饥饿**怎么办？低优先级永远等不到的话，SLA 谁负责？

**大师**：优先级是 **「同窗口抢锁」** 的 tie-breaker，不是万能插队票；该 **拆分队列** 的别硬挤一个 Scheduler。

**技术映射**：**Trigger 优先级与吞吐隔离**。

---

**小胖**：misfire 不就是晚了吗，晚跑一下不行？

**小白**：**合并、丢弃、立即补偿** 三种策略对 **资金类任务** 分别是啥后果？

**大师**：把 **业务幂等键** 与 **misfireInstruction** 绑在一起评审；没有幂等就别选「立刻全部补上」。

**技术映射**：**misfire 策略与业务一致性**。

---

**小胖**：`JobDataMap` 里塞个大 JSON 爽不爽？

**小白**：**序列化成本、版本升级、跨语言** 谁来买单？失败重试会不会把 **半截状态** 写回去？

**大师**：**小键值 + 外置大对象**；必须进 Map 的，**版本字段** 与 **兼容读** 写进规范。

**技术映射**：**JobDataMap 体积与演进策略**。

---

**小胖**：`@DisallowConcurrentExecution` 一贴我就安心了。

**小白**：**同 JobKey 串行** 会不会把 **补偿触发** 堵成长队？线程池够吗？

**大师**：先画 **并发模型草图**：哪些 Job 必须串行、哪些只是 **资源互斥**（应改用锁或分片）。

**技术映射**：**并发注解与队列时延**。

---

**小胖**：关机我直接拔电源，反正有下次触发。

**小白**：**在途 Job** 写了一半的外部副作用怎么算？**at-least-once** 下会不会双写？

**大师**：发布路径默认 **`shutdown(true)` + 超时**；`kill -9` 只能进 **混沌演练**，不进 **常规 Runbook**。

**技术映射**：**优雅停机与副作用幂等**。

---

**小胖**：Listener 里写业务逻辑最快了。

**小白**：Listener 异常会不会 **吞掉主流程** 或 **拖慢线程**？顺序保证吗？

**大师**：Listener 只做 **旁路观测与轻量编排**；重逻辑回 **Job** 或 **下游消息**。

**技术映射**：**Listener 边界与失败隔离**。

---

**小胖**：JDBC JobStore 不就是多几张表吗？

**小白**：**行锁、delegate、方言、索引** 哪个没对齐会出现 **幽灵触发** 或 **长时间抢锁**？

**大师**：把 **DB 监控**（慢查询、锁等待）与 **Quartz 线程栈** 对齐看；调参前先 **确认隔离级别与连接池**。

**技术映射**：**持久化 JobStore 与数据库协同**。

---

**小胖**：集群一开我就加节点，TPS 一定涨吧？

**小白**：**抢锁成本、心跳、instanceId** 乱配时，会不会 **越加越慢**？

**大师**：用 **压测曲线** 证明拐点；集群收益来自 **HA 与横向扩展边界**，不是魔法按钮。

**技术映射**：**集群伸缩与锁竞争**。

---

**小胖**：我想自定义 ThreadPool 秀一把。

**小白**：线程工厂、拒绝策略、上下文传递（MDC）**漏一项** 会出现啥线上症状？

**大师**：自定义可以，但要 **对齐 SPI 契约**与 **关闭语义**；否则 **泄漏线程** 比默认池更难查。

**技术映射**：**ThreadPool SPI 与生命周期**。
## 3 项目实战（约 1500–2000 字）

### 环境准备

打开 `JobStoreSupport`，搜索 **`lock`**、`Semaphore`、`acquireNextTriggers`。

### 分步实现

**步骤 1：目标** —— 在测试 DB 打开 **general log**（谨慎），观察 **acquire 与 release** SQL 序列。

**步骤 2：目标** —— 调整 **`org.quartz.jobStore.acquireTriggersWithinLock`**（若存在该键）对比延迟（以文档为准）。

**步骤 3：目标** —— 人为制造 **两节点同时抢**，观察 **一方等待超时**。

### 可能踩坑

| 坑 | 解决 |
| --- | --- |
| 长查询同连接 | 拆分业务连接 |
| 隔离级别过高 | 评估 RR vs RC |
| 监控缺失 | APM + DB 锁视图 |

### 完整代码清单

- [JobStoreSupport.java](../../quartz/src/main/java/org/quartz/impl/jdbcjobstore/JobStoreSupport.java)

### 测试验证

混沌：双节点 + 限流 DB IOPS。

## 4 项目总结（约 500–800 字）

### 优点与缺点（对比同类技术）

| 维度 | DB 行锁 | ZooKeeper 锁 | 纯应用锁 |
| --- | --- | --- | --- |
| 运维面 | 熟悉 DB 的团队低 | 中 | 低 |
| 一致性 | 强依赖 DB HA | 强 | 弱 |

### 适用 / 不适用场景

- **适用**：企业已有高可用 RDBMS。
- **不适用**：边缘无 DB 场景。

### 注意事项

- **主从延迟**：集群勿读从。
- **版本升级**：锁 SQL 变更。

### 常见踩坑（生产案例）

1. **FOR UPDATE 扫全表**：根因是缺索引。
2. **业务与调度同事务**：根因是边界错误。
3. **锁超时过短**：根因是拍脑袋。

#### 第32章思考题揭底

1. **RAM vs JDBC misfire**  
   **答**：**判定思路相近**（Trigger 更新路径共享），但 **RAM 无跨进程恢复**；**持久化与集群语义** 不同；**重启后 RAM 丢失全部状态**，misfire 「补救」空间不同。

2. **RAM 瓶颈 CPU 还是锁**  
   **答**：视规模：**触发器数量极大** 时 **锁竞争与数据结构操作** 常为主因；**Job 本身 CPU 密集** 时整体瓶颈可能转移到 **worker 线程池**；需 **profiling** 定论。

### 思考题（答案见下一章或 [答案索引](answers-index.md)）

1. `Semaphore` 在 JDBC JobStore 中的作用？
2. `StdRowLockSemaphore` 与 `SimpleSemaphore` 选型差异？

### 推广计划提示

- **测试**：锁等待用例。
- **运维**：DB 死锁告警。
- **开发**：下一章 `OperableTrigger` misfire 路径。
