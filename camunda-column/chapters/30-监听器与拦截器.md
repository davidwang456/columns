# 第 30 章：监听器与拦截器：Execution/Task 生命周期插桩

## 元信息

| 项目 | 内容 |
|------|------|
| 章节编号 | 第 30 章 |
| 标题 | 监听器与拦截器：Execution/Task 生命周期插桩 |
| 难度 | 高级 |
| 预计阅读 | 40～45 分钟 |
| 受众侧重 | 开发 |
| 依赖章节 | 第 6、21、23 章 |
| 环境版本 | `baseline-2026Q1` |

---

## 1. 项目背景

需要在**任务创建/分配/完成**或**执行流转**时写审计、同步外部系统、动态改 assignee——常用 **TaskListener、ExecutionListener** 与 **BpmnParseListener** 等扩展。本章要解决的**一条主线问题**是：选对监听点、控制**副作用与事务**，避免监听器里长调用拖死线程或导致**部分提交**（与第 23 章事务一致）。

---

## 2. 项目设计（三角色对话）

### 2.1 小胖开球

小胖说：「我在每个 Delegate 头尾复制粘贴日志不行吗？」

行，但横切关注点应集中；监听器减少重复，但要警惕**顺序**与**异常传播**。

### 2.2 小白追问

小白问：「第一，listener 抛错会怎样？第二，与 Spring 事件区别？第三，性能采样怎么做？」

### 2.3 大师定调

大师归纳：

- **异常**：可能回滚整个事务；需区分业务可恢复错误。
- **Spring 事件**：解耦但脱离流程事务语义时要小心。
- **性能**：避免 N+1 外部 HTTP；批量或异步。

### 2.4 何时用 Listener，何时用 Delegate？

小胖问：「那我把所有业务都写进 TaskListener 行不行？」大师答：**Delegate/服务任务**表达「完成一步业务动作」；**Listener**更适合**旁路审计、补数据、技术钩子**。若 listener 里写满业务规则，图上看不见，测试也难覆盖——这是典型**坏味道**。

---

## 3. 项目实战

### 3.1 环境前提

- 可在 BPMN 上配置 listener 或使用解析监听器。

### 3.2 步骤说明

1. 在任务 create 事件写审计日志（脱敏）。
2. 在 complete 前校验业务规则，不通过则抛异常阻止完成。
3. 压测对比加监听前后延迟。
4. 异常路径演练：审计写失败是否阻断流程（按需求）。
5. 文档化 listener 顺序与 owner。

### 3.3 源码与说明

**为什么慎用重逻辑 listener**：与主路径同线程，易放大延迟。

### 3.4 验证

- 审计完整；无意外双写；性能在预算内。

### 3.5 常见监听器清单（入门）

| 钩子 | 典型用途 | 风险 |
|------|----------|------|
| TaskListener create | 写入审计、计算候选人 | 远程调用超时 |
| TaskListener assignment | 通知、IM 推送 | 重复通知 |
| TaskListener complete | 汇总表单、校验 | 与事务边界耦合 |
| ExecutionListener start/end | 技术埋点 | 异常阻断流转 |

---

## 4. 项目总结

| 维度 | 内容 |
|------|------|
| 优点 | 横切关注点集中；扩展点清晰。 |
| 缺点 / 代价 | 过度使用难调试；顺序敏感。 |
| 适用场景 | 审计、动态分配、技术钩子。 |
| 不适用场景 | 核心业务全放 listener。 |
| 注意事项 | 事务、异常、性能。 |
| 常见踩坑 | listener 里调远程超时；隐式循环触发。 |

**延伸阅读**：第 21、23 章。

## 5. 附录：与拦截器（CommandInterceptor）分工

**Listener**偏业务生命周期；**CommandInterceptor**偏引擎命令层横切（如统一日志、租户校验）。平台型团队才会触及拦截器；业务项目优先 listener + 规范治理，避免深耦合内核升级路径。

## 6. 课后作业（可选）

1. 选一个 **TaskListener**，写出**异常时**是否应阻断完成的决策树。  
2. 给 listener 加重逻辑前，先压测 **P99 延迟**并设定预算。  
3. 列出 **禁止在 listener 做的事**（如长 HTTP、大事务）。  
4. 与日志规范对齐：listener 内打印哪些字段（脱敏）。  
5. 思考：若 listener 与 Delegate 都写审计，如何避免**双记**？  

## 7. 章末提要（面向推广口播）

监听器是「**手术刀**」：用得准是横切利器，用得滥会让排障变成拆毛线。统一原则：**listener 不写核心业务规则**，只写审计、通知、技术钩子；重逻辑进 Delegate 或领域服务。平台组才考虑拦截器；业务组坚持可见、可测。**若一次故障的根因是「listener 里 HTTP 卡死」**，请回到本章与第 23 章复查事务边界。

## 8. 深度追问（写给半年后的自己）

1. 团队 **Listener 白名单**里允许哪些副作用？禁止哪些？  
2. 若任务 create 与 complete 都写审计，如何避免重复与乱序？  
3. `notify` 类操作失败时，应阻断流程还是异步重试？谁拍板？  
4. 拦截器里做租户校验时，如何避免 **递归调用**？  
5. 能否给每个 listener **命名空间**前缀，便于日志检索？  
6. 若 Spring `@Async` 与 listener 同用，你如何论证事务一致性？  

**补白**：横切关注点一多，**顺序图**和 **owner** 比代码更重要；否则「谁加的监听」会变成「谁都不敢动」。

**版式补充（面向专栏编辑）**：本章在标准四段骨架之外，增设作业、提要、深度追问与补白，服务于「研发自学、运维周会、测试映射、管理 one-pager」四类读者。代码块、表格与命令片段用于对齐口径；若以「纯汉字」统计工具剔除技术片段，请改为以 **面授课 40 分钟**（含答疑）作为交付验收。编辑可将「章末提要」单独排版为 **电梯演讲**；将「深度追问」折叠为附录 PDF。推广部门如需短视频，可仅保留三角色对话与提要，时长约 8～12 分钟。**同一专栏内各章目录结构一致，便于内部知识库检索与自动化排版。** **字数验收提示**：以讲师连续朗读含标点约 **38～48 分钟**、听众能复述本章两个核心概念为达标；工具统计若剔除代码块与表格，字数可能波动，请以授课时长为准。
