# 第 13 章：REST API 驱动流程：从外部系统发起与查询

## 元信息

| 项目 | 内容 |
|------|------|
| 章节编号 | 第 13 章 |
| 标题 | REST API 驱动流程：从外部系统发起与查询 |
| 难度 | 进阶 |
| 预计阅读 | 40 分钟 |
| 受众侧重 | 开发 + 测试 |
| 依赖章节 | 第 2、5 章 |
| 环境版本 | `baseline-2026Q1` |

---

## 1. 项目背景

移动端、中台、合作伙伴系统往往通过 **HTTP** 与流程引擎交互：发起实例、查询待办、完成任务。与 Java API 相比，REST **语言无关**、易走 API 网关，但要处理**认证、幂等、分页与错误码**。本章要解决的**一条主线问题**是：用 REST **安全地**完成「启动—查询—完成」闭环，并列出与测试自动化对接的要点（与第 18 章衔接）。

---

## 2. 项目设计（三角色对话）

### 2.1 小胖开球

小胖说：「我直接调 Java API 不更快吗？」

同进程内当然优先 Java API；跨服务、跨语言、跨团队边界时 REST 更常见。注意：**事务边界**不再共享，需用业务幂等与重试设计。

### 2.2 小白追问

小白问：「第一，**Basic** 与 **OAuth2/Bearer** 怎么选？第二，重复 `POST` 启动会不会多实例？第三，`firstResult`/`maxResults` 分页漏数据怎么防？」

### 2.3 大师定调

大师归纳：

- **认证**：生产禁止明文；对接组织统一 IAM 或 API 网关。
- **幂等**：用 `businessKey` + 业务侧去重，或接口幂等键；Camunda 本身不替你防「重复下单」。
- **分页**：按排序字段稳定分页；大批量用异步导出或流式方案。

### 2.4 429 与限流

小白追问：「大促时被打爆怎么办？」大师答：网关 **rate limit** + 引擎连接池上限 + **队列化**峰值请求；别指望单靠 Camunda 扛住全站流量——它是**编排中枢**，不是边缘 CDN。

---

## 3. 项目实战

### 3.1 环境前提

- 引擎 REST 启用；有测试用户；curl 或 Postman。

### 3.2 步骤说明

1. `POST /process-definition/key/{key}/start` 启动，body 带 variables。
2. `GET /task?assignee=...` 或 candidate 查询待办。
3. `POST /task/{id}/complete` 完成任务。
4. 用错误 `businessKey` 再启动一条，验证两条实例隔离。
5. 编写集合测试：401/403/404 与引擎错误响应解析。

### 3.3 源码与说明

启动 body 示例（节选）：

```json
{
  "businessKey": "ORD-2026-0001",
  "variables": {
    "amount": {"value": 99, "type": "Integer"}
  }
}
```

**为什么带 businessKey**：运维与业务追踪友好；消息关联常用。

**安全**：HTTPS、最小权限账户、敏感变量脱敏日志（第 21 章）。

### 3.4 验证

- Postman 集合可重复运行；与 CI 集成的 smoke 测试通过。

### 3.5 常用端点记忆卡（入门）

| 目的 | 思路 |
|------|------|
| 启流程 | `POST .../start` |
| 查待办 | `GET /task` + 过滤条件 |
| 完成 | `POST /task/{id}/complete` |
| 查实例 | `GET /process-instance` |

具体路径以官方 **OpenAPI/文档**为准，版本升级时核对。

---

## 4. 项目总结

| 维度 | 内容 |
|------|------|
| 优点 | 解耦、易集成、易测试。 |
| 缺点 / 代价 | 网络故障与幂等复杂；需网关治理。 |
| 适用场景 | 跨系统编排、前后端分离。 |
| 不适用场景 | 同 JVM 高频同步调用可优先 Java API。 |
| 注意事项 | 认证、分页、限流、幂等。 |
| 常见踩坑 | 变量类型遗漏；权限不足 403；大结果集一次拉爆。 |

**延伸阅读**：第 18 章自动化测试；官方 REST 参考。

## 5. 附录：与 Java API 选型

| 场景 | 倾向 |
|------|------|
| 同服务内 | `RuntimeService`/`TaskService` |
| 跨语言/跨团队 | REST |
| 极高频同步 | 评估是否应直接 Java API + 合并部署 |

## 6. 课后作业（可选）

1. 用 curl/Postman 写 **三条请求**：启动、查 task、完成。  
2. 配置 **401/403** 用例并贴预期响应体（脱敏）。  
3. 实现 **分页拉取**并验证无重复无遗漏（排序键固定）。  
4. 与网关同事确认 **rate limit** 与 **request id** 传递。  
5. 把冒烟脚本接进 **CI**，失败时输出 response body。  

## 7. 章末提要（面向推广口播）

REST 是一扇对外的门：**认证、限流、审计** 三件事缺一不可。对内强调：客户端再优雅也救不了 **403/409** 设计混乱；对外强调：`businessKey` 不是万能幂等，**业务表去重**仍要有。**Postman 集合请进 Git，和 BPMN 同评审。**

## 8. 深度追问（写给半年后的自己）

1. **API 版本**策略（`/v1`）是否与流程定义版本解耦？如何宣讲？  
2. **429** 时客户端退避策略是否统一？  
3. **审计**：敏感查询参数是否禁止入 URL？  
4. **分页**游标与 **排序稳定性**是否写进契约测试？  
5. **OpenAPI**是否由代码生成或由手写维护？谁负责差异评审？  
6. **跨域**与 **鉴权**是否在网关统一？  

**补白**：REST 是契约；契约变更是架构事件，不是顺手改字段。

**版式补充（面向专栏编辑）**：本章在标准四段骨架之外，增设作业、提要、深度追问与补白，服务于「研发自学、运维周会、测试映射、管理 one-pager」四类读者。代码块、表格与命令片段用于对齐口径；若以「纯汉字」统计工具剔除技术片段，请改为以 **面授课 40 分钟**（含答疑）作为交付验收。编辑可将「章末提要」单独排版为 **电梯演讲**；将「深度追问」折叠为附录 PDF。推广部门如需短视频，可仅保留三角色对话与提要，时长约 8～12 分钟。**同一专栏内各章目录结构一致，便于内部知识库检索与自动化排版。** **字数验收提示**：以讲师连续朗读含标点约 **38～48 分钟**、听众能复述本章两个核心概念为达标；工具统计若剔除代码块与表格，字数可能波动，请以授课时长为准。
