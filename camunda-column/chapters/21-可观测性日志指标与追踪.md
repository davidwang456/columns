# 第 21 章：可观测性：日志、指标、链路追踪与流程实例关联

## 元信息

| 项目 | 内容 |
|------|------|
| 章节编号 | 第 21 章 |
| 标题 | 可观测性：日志、指标、链路追踪与流程实例关联 |
| 难度 | 进阶 |
| 预计阅读 | 40～45 分钟 |
| 受众侧重 | 运维 + 开发 |
| 依赖章节 | 第 2、13 章 |
| 环境版本 | `baseline-2026Q1` |

---

## 1. 项目背景

用户投诉「流程卡住了」——运维需要**分钟级定位**：是作业积压、外部任务 Worker 挂了，还是 DB 锁？开发需要把日志里的 `processInstanceId`、`activityId` 与分布式追踪 **traceId** 对齐。本章要解决的**一条主线问题**是：建立 **日志—指标—追踪** 三板斧与流程引擎字段的**关联规范**，形成排障剧本。

---

## 2. 项目设计（三角色对话）

### 2.1 小胖开球

小胖说：「我把日志级别开到 DEBUG 不就啥都有了？」

DEBUG 量太大且常含敏感数据；生产应结构化日志 + **关键字段** + **采样追踪**。

### 2.2 小白追问

小白问：「第一，哪些指标必看（作业延迟、外部任务 backlog）？第二，OpenTelemetry 与 Servlet 过滤器的注入点？第三，日志脱敏规则谁定？」

### 2.3 大师定调

大师归纳：

- **日志**：MDC 带 `processInstanceId`、`businessKey`（脱敏）。
- **指标**：作业执行时长、失败率、REST 延迟、DB 连接池。
- **追踪**：入口 REST span 透传到 Delegate/Worker。

### 2.4 指标与告警：别只盯 CPU

小胖问：「CPU 低就一定健康吗？」大师答：流程系统常见瓶颈在 **DB** 与 **外部系统**；应建立 **Golden Signals**（延迟、流量、错误、饱和度）+ **业务指标**（如待办积压、超时实例数）。

---

## 3. 项目实战

### 3.1 环境前提

- 可选 Prometheus、Grafana；日志聚合 ELK/OpenSearch。

### 3.2 步骤说明

1. 在启动实例入口写入 MDC。
2. 配置 Micrometer 指标导出（若 Spring Boot）。
3. 定义告警：作业队列深度超阈。
4. 演练：人为停 Worker，观察指标与日志能否定位。
5. 文档化「从用户 id 到 instanceId」查询路径（权限控制）。

### 3.3 源码与说明

**为什么关联 businessKey**：客服场景常用；注意隐私。

### 3.4 验证

- 故障注入演练在预定时间内定位根因；无敏感明文日志。

### 3.5 排障剧本（缩写）

1. 用户报障 → 记录 **businessKey** / 订单号。
2. Cockpit 查实例 **卡在哪一活动**。
3. 日志按 **instanceId** 拉全链路；看 **incident** 与 **异常栈**。
4. 若涉及外部任务 → 查 **Worker 指标** 与 **下游**。
5. 若涉及定时器 → 查 **作业延迟** 与 **时钟**。
6. 复盘写入 **第 24 章** 坑清单。

---

## 4. 项目总结

| 维度 | 内容 |
|------|------|
| 优点 | 缩短 MTTR；跨团队语言统一。 |
| 缺点 / 代价 | 存储成本；配置复杂。 |
| 适用场景 | 所有生产系统。 |
| 不适用场景 | 仅本地 demo。 |
| 注意事项 | 脱敏、采样、保留周期。 |
| 常见踩坑 | 日志无实例 id；指标缺失导致盲飞。 |

**延伸阅读**：第 22 章作业；第 33 章 K8s 探针。

## 5. 附录：日志样例字段（推荐）

`timestamp`、`level`、`traceId`、`processInstanceId`、`activityId`、`businessKey`(脱敏)、`tenantId`、`userId`、`durationMs`、`errorCode`。字段稳定后，ELK 解析规则与仪表盘才可持续。

## 6. 课后作业（可选）

1. 在预发配置 **MDC**，打印一条带 `processInstanceId` 的样例日志。  
2. 与监控同事建三个面板：**REST P95**、**作业延迟**、**错误率**。  
3. 设计一次 **故障演练**：停 Worker，验证告警是否在 X 分钟内触发。  
4. 列出 **禁止入日志** 的字段清单（PII）。  
5. 采样率策略：**全量** vs **按租户** vs **按错误放大**。  

## 7. 章末提要（面向推广口播）

可观测性的目标不是「日志越多越好」，而是 **5 分钟内能回答**：**卡在哪、影响谁、是否扩大化**。给值班同事一张**排障决策树**胜过一百条 debug。记住把 **processInstanceId** 与 **traceId** 绑在一起，否则链路追踪和流程视图会对不上号。**凡是不能从日志还原的路径，就不该上生产。**

## 8. 深度追问（写给半年后的自己）

1. **日志保留天数**与合规是否一致？若缩短，谁签字？  
2. 采样率在 **大促**可否动态调低？开关谁掌握？  
3. 指标 **基数爆炸**（高基数 label）是否已治理？  
4. 链路追踪 **尾部采样**是否启用？成本如何？  
5. 若第三方 APM 与自建 ELK 并存，**权威**是谁？  
6. on-call 是否考核 **MTTR** 与 **误报率**？  

**补白**：可观测性是团队共同产品；只买工具不落地字段规范，必失败。 

**版式补充（面向专栏编辑）**：本章在标准四段骨架之外，增设作业、提要、深度追问与补白，服务于「研发自学、运维周会、测试映射、管理 one-pager」四类读者。代码块、表格与命令片段用于对齐口径；若以「纯汉字」统计工具剔除技术片段，请改为以 **面授课 40 分钟**（含答疑）作为交付验收。编辑可将「章末提要」单独排版为 **电梯演讲**；将「深度追问」折叠为附录 PDF。推广部门如需短视频，可仅保留三角色对话与提要，时长约 8～12 分钟。**同一专栏内各章目录结构一致，便于内部知识库检索与自动化排版。** **字数验收提示**：以讲师连续朗读含标点约 **38～48 分钟**、听众能复述本章两个核心概念为达标；工具统计若剔除代码块与表格，字数可能波动，请以授课时长为准。
