# 第 31 章：源码导读（一）：启动与部署解析

## 元信息

| 项目 | 内容 |
|------|------|
| 章节编号 | 第 31 章 |
| 标题 | 源码导读（一）：启动与部署解析 |
| 难度 | 高级 |
| 预计阅读 | 45～50 分钟 |
| 受众侧重 | 开发 |
| 依赖章节 | 第 2 章 |
| 环境版本 | 阅读 `camunda-engine` **7.20.x**（与 `VERSIONS.md` 对齐） |

---

## 1. 项目背景

排障「为什么没部署成功」「Bean 没注入」时，**翻源码**比猜配置快。本章要解决的**一条主线问题**是：沿着 **Spring Boot Starter** 初始化 **ProcessEngineConfiguration**、**部署扫描**、**JobExecutor** 启动的路径，标出**关键类名**（版本以 7.20.x 为例，升级时自行 diff）。

---

## 2. 项目设计（三角色对话）

### 2.1 小胖开球

小胖说：「源码几万行，从哪看起？」

从 **启动入口** 与 **自动配置类** 起，跟到 `ProcessEngine` bean 创建；不要一上来读执行树。

### 2.2 小白追问

小白问：「第一，与纯 `camunda.cfg.xml` 差异？第二，哪些 Bean 可覆盖？第三，升级小版本关注点？」

### 2.3 大师定调

大师归纳：

- **Spring Boot** 路径：`CamundaBpmAutoConfiguration`（类名以实际版本为准）。
- **扩展**：通过 `ProcessEnginePlugin` 注入自定义行为。
- **升级**：看 release notes 中 **database schema** 与 **配置项** 变更。

### 2.4 小胖：「Bean 都有了为啥还部署失败？」

大师答：常见原因包括 **BPMN XML 不合法**、**资源未进类路径**、**租户/权限**限制、**数据库迁移未执行**。源码阅读时优先搜 **Deployment** 相关异常栈，再回退到资源扫描配置。

---

## 3. 项目实战

### 3.1 环境前提

- IDE 可下载 sources；依赖与专栏 `VERSIONS.md` 一致。

### 3.2 步骤说明

1. 在 IDE 中导航 `camunda-bpm-spring-boot-starter` 自动配置。
2. 跟踪创建 `ProcessEngine` 的工厂方法。
3. 找到 `deployments` 相关调用，对应自动部署。
4. 记录 `JobExecutor` 启动位置与默认线程配置。
5. 画一张**启动时序草图**（类名列表）团队内分享。

### 3.3 源码与说明

**为什么标注版本**：同名类在不同版本可能有包调整。

**勿抄类名进生产文档而不写版本**：易误导后人。

### 3.4 验证

- 能解释一次「部署失败」日志从哪抛出；能找到覆盖点。

### 3.5 建议笔记模板（团队内部分享）

- 版本：`camunda-engine` __.__.__  
- 启动链：AutoConfiguration → `ProcessEngine` Bean → `RepositoryService` 部署  
- 自定义点：`ProcessEnginePlugin` / `ProcessEngineConfiguration` bean  
- 本次排障结论：_______  

---

## 4. 项目总结

| 维度 | 内容 |
|------|------|
| 优点 | 深度排障与定制基础。 |
| 缺点 / 代价 | 时间成本高；版本漂移。 |
| 适用场景 | 资深开发、平台组。 |
| 不适用场景 | 仅需业务建模。 |
| 注意事项 | 绑定源码版本；升级重读。 |
| 常见踩坑 | 用旧文档对新版本。 |

**延伸阅读**：第 32 章执行树；官方 GitHub。

## 5. 附录：不建议新手阅读的包

内核并发、命令拦截器全链路过长；除非要做**二开平台**，否则优先 **官方文档 + 第 32 章** 建立直觉，再决定是否深入 `org.camunda.bpm.engine.impl`。

## 6. 课后作业（可选）

1. 在 IDE 打开自动配置类，截一张 **Bean 装配**关系图（手绘）。  
2. 记录 **数据库迁移**在启动链路的哪一步执行。  
3. 对比官方文档：列出你所用版本新增的 **配置项** 三条。  
4. 写一页 **升级 Runbook**：从 7.19 到 7.20（示例版本）要改什么。  
5. 思考：`ProcessEnginePlugin` 可用来加什么团队级横切能力？  
