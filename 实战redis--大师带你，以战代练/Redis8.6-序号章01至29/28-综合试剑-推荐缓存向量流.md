# 第 28 章 · 综合试剑：推荐、缓存、向量与流

本故事纯属虚构，如有雷同，纯属巧合。

> **版本说明**：架构示意；命令以 **8.6** 与模块实际可用性为准。

## 本话目标

- 用 **一张草图**串起：缓存、兴趣、点击流、向量、订单。  
- 每条链路写出 **一个失败模式 + 兜底**。

## 步步引导：先画块，再画箭

**小白**：我先把 Kafka、K8s、微服务全画上……

**大师**：停。**业务数据落哪、谁可丢、谁不可丢**没清，画布越大越糊。

**小白**：点击流和订单流放一个 Stream？

**大师**：可以，但要想清 **消费者、权限、保留策略**；常 **分 key** 或 **订单走专用 MQ**。

**小白**：向量与详情都存在 Redis？

**大师**：常见是 **向量 + 指针**，详情在 DB/对象存储；全塞 Redis 先算 **内存账**。

## 小剧场：一桌菜

冷盘、热炒、甜品各用合适的盘——**一个 Stream 装天下**往往烫手。

---

## 一页纸架构题

**大师**：电商「猜你喜欢」+ 实时点击埋点 + 订单事件。**不要求**上 K8s，要求**说清数据落点、读写路径、失败模式**——架构评审里要听你**像负责人一样说话**，不像背书童子。

**小白**：弟子试试……

**大师**：先画四方块：**接入层 / 应用 / Redis / 下游（DB、模型、MQ）**，再画箭头：**谁是主、谁是辅、谁可丢、谁不可丢**。

---

## 建议拆分（示例）

| 职责 | Redis 能力 | 备注 |
|------|------------|------|
| 商品详情缓存 | String / Hash + TTL | 热点 key 走 [25-可观测与HOTKEYS.md](25-可观测与HOTKEYS.md) |
| 用户短期兴趣 | ZSet（带时间衰减 score）或 Stream 聚合 | 与产品定义「兴趣半衰期」 |
| 向量召回 | Vector set `VADD`/`VSIM` | 嵌入离线/准实时更新；见 [20-VectorSet与RAG迷你管道.md](20-VectorSet与RAG迷你管道.md) |
| 点击流 | Stream；生产端 **IDMP** 防重 | 见 [13-Stream-消费组与XADD幂等.md](13-Stream-消费组与XADD幂等.md) |
| 订单事件 | Stream 另一 key 或业务 Kafka | **Redis 不是万能 MQ**（[15-List与Stream混合战.md](15-List与Stream混合战.md)） |
| 语义缓存 | [21-语义缓存与路由.md](21-语义缓存与路由.md) | 阈值 + 版本 + 抽检 |

---

## 失败模式（评审必问）

- 主从切换：**重复消费**与**写丢失窗口**（[23-持久化与副本.md](23-持久化与副本.md)）  
- 大促：**热 key** 与 **带宽**（[25-可观测与HOTKEYS.md](25-可观测与HOTKEYS.md)、[27-集群与高可用.md](27-集群与高可用.md)）  
- 向量：**维数变更**、索引重建计划、双写迁移  
- 淘汰：**缓存穿透**与 **lrm/LRU 策略**（[24-淘汰策略-LRU到LRM.md](24-淘汰策略-LRU到LRM.md)）

---

## 口述模板（可直接练）

1. **Happy path**：用户请求 → 缓存命中 / 向量召回 → 返回。  
2. **降级**：模型超时 → 规则推荐 → 静态榜单。  
3. **观测**：QPS、P99、Stream lag、向量命中率。  
4. **安全**：TLS、ACL（[26-安全-ACL与TLS.md](26-安全-ACL与TLS.md)）。

---

## 动手试一试（白板即可）

1. A4 纸横过来：**四格**写 接入 / 应用 / Redis / 下游。  
2. 用三种颜色画箭：**读路径、写路径、异步路径**。  
3. 每条路径旁写一个字：**可丢** 或 **不可丢**。  
4. 找一位同事 **90 秒复述**，听不懂的地方就是你的漏洞。

## 实战锦囊

- 评审时带 **数字**：QPS、P99、lag、内存上限，至少各一个。  
- **降级路径**写进 Runbook，别只写在 PPT 备注里。

---

## 收式

**小白**：弟子这一页纸能折成暗器吗？

**大师**：能，折成**面试时的底气**。最后一章：[29-面试篇-铜人阵2.md](29-面试篇-铜人阵2.md)。
