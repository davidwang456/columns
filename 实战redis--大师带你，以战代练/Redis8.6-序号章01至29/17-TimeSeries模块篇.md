# 第 17 章 · Time series：模块篇

本故事纯属虚构，如有雷同，纯属巧合。

> **版本说明**：Time series 以你本机 `COMMAND` 与官方文档为准；**8.6** 发行说明含 **NaN**、`COUNTNAN`、`COUNTALL` 等增量，写作时核对当前标签。

## 本话目标

- 说清：**时序数据为何忌用「字符串糊墙」**。  
- 设计 **label** 时避开高基数陷阱。

## 步步引导：指标不是键名堆出来的

**小白**：我每个点一个 key：`metric:host:1:cpu:...`

**大师**：短期能跑；规模上来 **key 数量爆炸**。时序模块用 **样本 + 标签** 管维度。

**小白**：我把 traceId 当 label？

**大师**：**高基数**来了——磁盘与查询一起炸。label 应是**有界维度**。

**小白**：NaN 算啥？

**大师**：**不是没采样**，是「这一拍无效」；聚合时要懂 **COUNTNAN/COUNTALL** 与业务含义是否一致。

## 小剧场：更夫账本

更夫不只喊「三更天」，还要记「这一更响了几次锣」——**时序是账本，不是单句台词**。

---

## 传感器与 SLA 曲线

**大师**：监控场景里，**时序**比「字符串里塞时间戳」干净：**降采样、规则、聚合**一条线。别用 Hash 假装时序库到**上亿点**才喊疼——那时神仙难救。

**小白**：模块从哪加载？

**大师**：先看 **`MODULE LIST`** 与镜像/构建说明；再读 [04-编译秘笈-BUILD与模块对照表.md](04-编译秘笈-BUILD与模块对照表.md)。云厂商是否内置、是否裁剪，**以控制台为准**。

**趣味比方**：时序模块像**更夫打更**——不仅要报「几点了」，还要能告诉你「这一更里出了几次妖」。

---

## 实战心智

- **Label 设计**决定后期查询维度（机房、服务、实例、指标名）；**上线后改标签**等于换账本。  
- **降采样规则与保留策略**提前定：原始精度保留多久、分钟级保留多久，别等磁盘满才开会。  
- **8.6** 若业务允许 NaN，理解 **`COUNTNAN`/`COUNTALL`** 与旧聚合差异——**NaN 不是「没数据」**，是「数据说不知道」。

---

## 与其他栈对照（口头即可）

| 场景 | Redis TimeSeries | 专用 TSDB |
|------|------------------|-----------|
| 与业务缓存同集群、低延迟 | 合适 | 过重 |
| 超长期海量、复杂分析 | 需评估 | 常更合适 |
| 与告警系统联动 | 看导出/查询能力 | 常更成熟 |

---

## 踩坑

- 高基数 label（每个请求一个 trace id）→ **爆炸**。  
- 与 Grafana/Prometheus 混用：弄清**谁是主存储**。  
- 备份恢复：模块数据走 **RDB/AOF** 与厂商策略，**演练过再吹**。

---

## 动手试一试

1. `MODULE LIST` 确认是否加载时序模块。  
2. 按官方文档创建一个 **测试序列**，写入若干点，再做一个 **降采样/查询**（命令以文档为准）。  
3. 故意写一个 **高基数 label**，观察存储或查询恶化——然后删掉重来。

## 实战锦囊

- **保留策略**与**降采样**在画图前定，别等磁盘满。  
- 与 Prometheus/Grafana 混用时，写清 **主存储是谁**，避免「双写双真相」。

---

## 收式

**小白**：弟子先去把 label 设计写好再建序列。

**大师**：下一章：[18-JSON类型.md](18-JSON类型.md)。
