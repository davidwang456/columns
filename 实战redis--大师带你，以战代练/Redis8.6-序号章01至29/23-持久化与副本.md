# 第 23 章 · 持久化与副本

本故事纯属虚构，如有雷同，纯属巧合。

> **版本说明**：RDB/AOF 语义长期稳定；**Multi-part AOF** 等以当前 `redis.conf` 与文档为准。

## 本话目标

- 用一句话比较 **RDB 与 AOF** 的丢数风险与性能。  
- 解释 **异步复制**下「主写入成功」不等于「从已安全」。

## 项目背景

**贯穿设定**：**某电商平台**把商品目录、购物车、会话等放在 Redis：**宕机与切换**时能否找回数据，取决于**持久化与复制**怎么配，而不是「用了 Redis 就自动不丢」。

**与本章关系**：**RDB** 与 **AOF** 对应不同的 **RPO** 与开销；主从**异步复制**下，主库写入成功**不等于**从库已持久可切换。须与业务对齐 **RPO/RTO**，再选配置组合。

## 步步引导：备份不是许愿

**大师**：听到「我们开了持久化」，你心里先浮现什么画面？

**小白**：磁盘上有备份，心里踏实，应该**不会丢**吧？

**大师**：踏实的方向对。你再往**时间缝**里看一眼：**两次快照之间**、**日记落盘的节奏**、**换主那一刻**——有没有**还来不及誊清**的空白？

**小白**：……有。那怎么说才严谨？

**大师**：先请业务说一句**能忍受丢多久**（RPO）、**能停多久**（RTO）——数字落地，话就好说。

**小白**：我把 `appendfsync` 设成 always，是不是最稳？

**大师**：**稳**与**快**常常坐在跷跷板两端。你不妨用压测量：**磁盘与延迟**愿不愿意换这份稳——愿意，再选。

**小白**：从库跟不上，我把读都打回主库？

**大师**：救急可以。你再想**主库肩上的担子**与**以后人更多**时怎么办——**根因**（链路与热点）终究要排。

## 小剧场：书童掉线

书童抄书掉线几行——主家问：「那几行写进原著没？」**切换主从**就是在换书童，**窗口期**要问清。

---

## 案底与抄书

**大师**：**RDB** 像定期**拍照**，恢复快、可能丢一段；**AOF** 像**写日记**，更耐丢、体积与 fsync 要算账。主从复制是**抄书**——抄得快不算，**断线重连、部分重同步、积压缓冲区**才算入门。

**小白**：会丢数据吗？

**大师**：**会**。`appendfsync always` 与性能权衡、`WAIT` 与副本确认、**故障切换窗口**——面试专爱考这个。说「Redis 绝不丢」的，下一题就不用答了。

**趣味比方**：RDB 像**年终画像**；AOF 像**逐日起居注**；复制像**书童抄书**——书童掉线那几页，主家写没写上，要讲道理。

---

## 实战检查

- `INFO persistence` —— AOF/RDB 是否开启、上次状态。  
- `LASTSAVE`、`redis-check-aof`（修复演练在**测试环境**）。  
- 磁盘：AOF rewrite 峰值、fork 延迟（`INFO stats` 里的相关项）。  
- 云托管场景：**责任边界**读服务商 SLA，别自建思维硬套。

---

## 复制与一致性心智

- **异步复制**是默认故事：主已写、从未确认，切换时可能丢「最后一截」。  
- `WAIT`：等副本落盘/确认的数量，**延迟换安全**。  
- **哨兵/集群切换**：应用重试、幂等、读从延迟——见 [27-集群与高可用.md](27-集群与高可用.md)。

---

## 深度挂钩

[`src/rdb.c`](../../src/rdb.c)、[`src/aof.c`](../../src/aof.c)、`replication.c` 相关逻辑。

---

## 动手试一试（练习环境）

```text
INFO persistence
INFO replication
```

改 `redis.conf`（练习机）：切换 **RDB save 策略** 或 **AOF 开关**（按文档），重启后对比 `INFO persistence`；用 `redis-cli --rdb` 或 `BGSAVE` 观察 **fork 延迟**（看 `latest_fork_usec` 等）。

## 实战锦囊

- 云托管：**备份谁做、恢复谁按按钮**，写进 Runbook。  
- 演练 **`redis-check-aof`** 只在测试环境做熟。  
- 与业务约定 **`WAIT` 用法**或**异步容忍**，别口头「强一致」。

---

## 收式

**小白**：弟子先去 `INFO persistence` 抄三遍。

**大师**：下一章：[24-淘汰策略-LRU到LRM.md](24-淘汰策略-LRU到LRM.md)。
