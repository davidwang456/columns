本故事纯属虚构，如有雷同，纯属巧合。

> **版本说明**：编码随版本演进；以 `OBJECT ENCODING` 与源码 `tryEncoding` 分支为准。

---

## 内外结构 8.x：listpack 时代的内功

**大师**：旧文讲 ziplist、linkedlist；如今主线是 **listpack** 等更紧凑、更少边界的编码。**Hash** 在字段少时可走紧凑路径，字段多或带 **字段 TTL** 时行为更复杂——**别死记一张表**。

**小白**：我 `DEBUG OBJECT` 行不行？

**大师**：**生产慎用**；开发机配合 `OBJECT ENCODING`、`MEMORY USAGE`。

---

## 推荐阅读顺序（本地源码）

1. [`src/listpack.c`](d:/software/workspace/redis/src/listpack.c) — 紧凑序列如何扩。  
2. [`src/t_hash.c`](d:/software/workspace/redis/src/t_hash.c) — Hash 编码与过期。  
3. [`src/t_zset.c`](d:/software/workspace/redis/src/t_zset.c) — 跳表 + dict。

---

## 收式

下一篇：[卷六-23-持久化与副本.md](卷六-23-持久化与副本.md)。
