本故事纯属虚构，如有雷同，纯属巧合。

> **版本说明**：Time series 以你本机 `COMMAND` 与官方文档为准；**8.6** 发行说明含 **NaN**、`COUNTNAN`、`COUNTALL` 等增量，写作时核对当前标签。

---

## Time series：传感器与 SLA 曲线

**大师**：监控场景里，**时序**比「字符串里塞时间戳」干净：**降采样、规则、聚合**一条线。别用 Hash 假装时序库到**上亿点**才喊疼。

**小白**：模块从哪加载？

**大师**：先看 **`MODULE LIST`** 与镜像/构建说明；再读 [卷〇-附录](卷〇-附录-模块与BUILD_WITH_MODULES对照表.md)。

---

## 实战心智

- Label 设计决定后期查询维度。  
- 降采样规则与保留策略**提前定**，别等磁盘满。  
- **8.6** 若业务允许 NaN，理解 **`COUNTNAN`/`COUNTALL`** 与旧聚合差异。

---

## 收式

卷五：[卷五-18-JSON类型.md](卷五-18-JSON类型.md)。
