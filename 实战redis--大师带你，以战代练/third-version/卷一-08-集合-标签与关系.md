本故事纯属虚构，如有雷同，纯属巧合。

> **版本说明**：Set 经典命令；大数据集注意 **SUNION** 等复杂度。

---

## 集合：去重、关系、六度人脉的玩具箱

**大师**：**`SADD`/`SISMEMBER`/`SINTER`/`SUNION`**——标签、好友关系、共同关注，一套够用。旧文「六度空间」趣味题可保留：**BFS 在应用层，Redis 存邻接集合**。

**小白**：Set 能分页吗？

**大师**：用 **`SSCAN`**，别 `SMEMBERS` 一把梭。

---

## 实战要点

- 布隆过滤器替代方案：巨量去重可上 **Set**，内存顶不住则换 [卷二-12](卷二-12-概率数据结构-模块篇.md) 的 Bloom（模块）。  
- 随机抽奖：`SRANDMEMBER` / `SPOP`，注意是否放回。

---

## 深度挂钩

[`src/t_set.c`](d:/software/workspace/redis/src/t_set.c)。

---

## 收式

下一篇：[卷一-09-有序集合-排行榜与延时.md](卷一-09-有序集合-排行榜与延时.md)。
