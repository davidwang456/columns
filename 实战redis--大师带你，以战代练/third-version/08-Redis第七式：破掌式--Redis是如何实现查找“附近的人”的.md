本故事纯属虚构，如有雷同，纯属巧合。

> 在中国众多武术流派中，以“掌”为最的当说八卦门。“八卦”最早见于《周易》：“两仪生四象，四象生八卦”。八卦掌是一种以掌法变换和行步走转为主的拳术。它将武功与导引吐纳溶为一体，内外兼修。不仅有强身健体之功用，而且能够锻炼攻防搏击的技能，享誉武林。破掌式破解诸般拳脚指掌上功夫， 长拳 短打 、 擒拿 点穴 、 鹰爪 虎爪 、 铁沙掌 种种 拳脚功夫 。

**故事背景**

![](http://p9.toutiaoimg.com/large/pgc-image/c1df1bd44d3c4c7bb8b3c3b1c8d9306c)

《功夫》

> 如来神掌相传是如来佛祖为了普度众生，解劫除厄，传与凡人的一套绝世武学，神掌威力可敌天灾地祸，降妖除魔。要说这秘笈的来处，是阿星小时候从老乞丐那里花 10 元钱买来的，虽然标价只有 2 分钱，却也买得比较值，当时没练成功，为救哑女还挨顿揍，但是长大后受伤竟然能自动痊愈，应该是内功已到了一定程度，最后破茧成蝶，终于大功告成。老乞丐的话就曾有预言“……简直百年一见的练武奇才啊，如果有一天让你打通任督二脉，你还不飞龙上天……”。

![](http://p9.toutiaoimg.com/large/pgc-image/2c824653262c40529a235efe01dadf8e)

**Redis 第七式：破掌式**

**大师**：我们现在的用户规模为 1kw，假设为了增加我们系统用户的粘性，想在系统中增加一个查找附近的会员功能，如何实现呢？

**小白**：大师，我只知道比较笨的方法，就是用户打开我们系统的时候，我们采集他的经度和纬度，上传到数据库，然后可根据球面两地距离公式判断用户的远近。

**大师**：这个方法有一定的可行性，但数据库如 MySQL、PostgreSQL 都有存储引擎支持地理位置（GEO）。例如 MySQL 可以这样做：

SET @pt1 = ST_GeomFromText('POINT(116.405289 39.904987)');

> SELECT *,ST_Distance_Sphere(ST_GeomFromText(CONCAT('POINT(',lng,' ',lat,')')), @pt1) as distance FROM pb_area
> 
> WHERE ST_Distance_Sphere(ST_GeomFromText(CONCAT('POINT(',lng,' ',lat,')')), @pt1) < 10000 ORDER BY distance

PostGIS 是 PostgreSQL 关系数据库的空间数据库扩展。增加了对地理类型的支持，允许在 SQL 中运行位置查询。

**大师**：使用关系型数据库可以解决这个问题，但这样会导致数据库的压力比较大，一方面，我们的主系统依赖数据库，另外一方面我们有 1000w 的用户，如何解决用户经纬度在不断更新，用户查找频繁的问题呢？

**小白**：可以通过缓存的方式，缓存一部分结果，但这样治标不治本，请大师给些提示。

**大师**：其实这是我们今天要学习的 Redis 第七式：破掌式。它的招式比较少，容易学会。

> Redis 在 3.2 版本以后增加了地理位置的处理，其提供了 6 个地理位置相关的命令
> 
> 1. GEOADD

**时间复杂度：**每一个元素添加是 O(log(N)) ，N 是 sorted set 的元素数量。

> 将指定的地理空间位置（纬度、经度、名称）添加到指定的 key 中。这些数据将会存储到 sorted set 这样的目的是为了方便使用 GEORADIUS 或者 GEORADIUSBYMEMBER 命令对数据进行半径查询等操作。
> 
> 该命令以采用标准格式的参数 x,y,所以经度必须在纬度之前。这些坐标的限制是可以被编入索引的，区域面积可以很接近极点但是不能索引。具体的限制，由 EPSG:900913 / EPSG:3785 / OSGEO:41001 规定如下：
> 
> 有效的经度从-180 度到 180 度。
> 
> 有效的纬度从-85.05112878 度到 85.05112878 度。
> 
> 当坐标位置超出上述指定范围时，该命令将会返回一个错误。
> 
> 2.GEOPOS 从键里面返回所有给定位置元素的位置（经度和纬度）

```
示例：
redis> GEOADD Sicily 13.361389 38.115556 "Palermo" 15.087269 37.502669 "Catania"
(integer) 2
redis> GEOPOS Sicily Palermo Catania NonExisting
1) 1) "13.361389338970184"
 2) "38.115556395496299"
2) 1) "15.087267458438873"
 2) "37.50266842333162"
3) (nil)
redis> 
```

> 3.GEODIST
> 
> **时间复杂度：**O(log(N))
> 
> 返回两个给定位置之间的距离。
> 
> 如果两个位置之间的其中一个不存在， 那么命令返回空值。
> 
> 指定单位的参数 unit 必须是以下单位的其中一个：
> 
> **m** 表示单位为米。
> 
> **km** 表示单位为千米。
> 
> **mi** 表示单位为英里。
> 
> **ft** 表示单位为英尺。
> 
> 如果用户没有显式地指定单位参数， 那么 GEODIST 默认使用米作为单位。
> 
> GEODIST 命令在计算距离时会假设地球为完美的球形， 在极限情况下， 这一假设最大会造成 0.5% 的误差。
> 
> 4.GEORADIUS
> 
> **时间复杂度：**O(N+log(M))
> 
> 以给定的经纬度为中心， 返回键包含的位置元素当中， 与中心的距离不超过给定最大距离的所有位置元素。
> 
> 范围可以使用以下其中一个单位：
> 
> **m** 表示单位为米。
> 
> **km** 表示单位为千米。
> 
> **mi** 表示单位为英里。
> 
> **ft** 表示单位为英尺。
> 
> 在给定以下可选项时， 命令会返回额外的信息：
> 
> WITHDIST: 在返回位置元素的同时， 将位置元素与中心之间的距离也一并返回。 距离的单位和用户给定的范围单位保持一致。
> 
> WITHCOORD: 将位置元素的经度和纬度也一并返回。
> 
> WITHHASH: 以 52 位有符号整数的形式， 返回位置元素经过原始 geohash 编码的有序集合分值。 这个选项主要用于底层应用或者调试， 实际中的作用并不大。
> 
> 命令默认返回未排序的位置元素。 通过以下两个参数， 用户可以指定被返回位置元素的排序方式：
> 
> ASC: 根据中心的位置， 按照从近到远的方式返回位置元素。
> 
> DESC: 根据中心的位置， 按照从远到近的方式返回位置元素
> 
> 5.GEORADIUSBYMEMBER
> 
> **时间复杂度：**O(N+log(M))
> 
> 这个命令和 GEORADIUS 命令一样， 都可以找出位于指定范围内的元素， 但是 GEORADIUSBYMEMBER 的中心点是由给定的位置元素决定的， 而不是像 GEORADIUS 那样， 使用输入的经度和纬度来决定中心点
> 
> 指定成员的位置被用作查询的中心。
> 
> 6.GEOHASH
> 
> **时间复杂度：**O(log(N))
> 
> 返回一个或多个位置元素的 Geohash 表示。通常使用表示位置的元素使用不同的技术，使用 Geohash 位置 52 点整数编码。

**小白**：大师，我怎么觉得 GEOHASH 没有什么作用呢？

**大师**：GEOHASH 返回的其实是元素的经纬度经过 Geohash 计算后的 base32 编码字符串。可以在浏览器中打开 `http://geohash.org/${hash}` 直接定位，它是 Geohash 的标准编码值。其原理比较容易理解，核心思想是把二维经纬度映射成一维编码，便于做区间检索与邻近搜索。

**小白**：大师，Redis 的这个功能太棒了，我知道该怎么做了。

**番外：GEO 为什么没有单独的 type？**

**大师**：`TYPE` 看 Geo key 往往是 **`zset`**。实现上 **`t_geo.c` 把经纬度编码进 52 位 GeoHash 整数作为 score**，member 仍是地点名；`GEORADIUS` 等命令在半径查询时解码、算距、过滤。所以你用 `ZRANGE` 直接捅同一个 key，看到的是编码后的 score，而不是直观经纬度——这是为把二维近邻搜索挂到有序集合的排序与范围能力上。

**小白**：那我数据量特别大时瓶颈在哪？

**大师**：半径查询复杂度与集合规模相关（文档里写的 O(N+log(M)) 类模型要牢记）；热点区域可 **分片 key、多 cell、或换 Elasticsearch / PostGIS** 做二级索引。读 `t_geo.c` 里 `geoaddCommand`、`georadiusGeneric`，你能对上官网文档里的“经度在前、纬度在后”和合法纬度范围校验从何而来。

**大师**：其实，不仅仅是 Redis 有这种招式，其它诸如 MongoDB、Elasticsearch 等也有类似的招式。目前 GEO 常用的解决方案有：

- MySQL 自定义函数计算
- MySQL GEO 索引
- mongodb geo 索引
- postgresql PostGis 索引
- Redis geo
- ElasticSearch

好了，今天的 Redis 第七式：破掌式就到这里，你回去后好好复习一下

**小白**：恭送大师！
