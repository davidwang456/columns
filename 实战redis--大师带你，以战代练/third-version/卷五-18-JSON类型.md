本故事纯属虚构，如有雷同，纯属巧合。

> **版本说明**：**原生 JSON** 类型与命令以官方文档为准；与「字符串塞 JSON」兼容迁移需双写或离线转换。

---

## JSON：别再把文档塞进字符串里硬啃

**大师**：旧招是 `SET doc '{"a":1}'`，查询全靠应用反序列化。**新招**是 **JSON 类型 + JSONPath**——更新子字段、类型校验、与索引联动（若启用 Query Engine）。

**小白**：我还要字符串版吗？

**大师**：迁移期可以**双写**；稳定后**删字符串 key**，留 JSON key。

---

## 实战要点

- 路径表达式：按文档版本使用 **JSONPath** 语法。  
- 与 [卷五-19](卷五-19-RedisQueryEngine导论.md)：**有索引才有「数据库感」**；无索引仍是「结构化 blob」。

---

## 收式

下一篇：[卷五-19-RedisQueryEngine导论.md](卷五-19-RedisQueryEngine导论.md)。
