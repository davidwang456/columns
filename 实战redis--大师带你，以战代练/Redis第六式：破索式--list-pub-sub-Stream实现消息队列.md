本故事纯属虚构，如有雷同，纯属巧合。

> 在兵器家族中，有一批独特而声名显赫的成员，她们与这个家族的大多数凭借锋利的刃尖或惊人的重量杀伤敌人的兵器不同，而是“以柔克刚”，利用自身的“柔软”来达到杀伤敌人的目的，她们就是独步一方的软兵器。
> 
> 破解诸般长索、短鞭、三节棍、鍊子枪、铁链、渔网、飞锤、流星种种软兵刃。

故事背景

《西游记》

> 幌金绳是《西游记》里太上老君身上隐藏的法宝之一，通体呈金色，表面上看只是老君用来勒袍的腰带，后来被两个看炉子的童儿偷走带去了下界，金角银角将幌金绳给了娘亲九尾狐保管，却被孙悟空给抢走了，反用来对付他俩，可孙悟空没有想到的是，这幌金绳除了有捆人的咒语外，还有松绳的咒语，又被银角大王给捆了。

**Redis第六式：破索式**

**大师**：在用户创建一个账户的同时，我们往往会给用户发送一封欢迎信息，并给用户的账户赠送积分，如果你在设计创建用户的时候，你会怎么做？

**小白**：大师，这种项目我做过，创建一个数据库事务，把insert账号，insert message，和赠送积分update 放到事务里，一起提交。

**大师**：针对用户量比较小，使用场景少的系统，这种做法没有问题。如果现在你在一个中型的互联网公司，你会怎么做呢？

**小白**：大师，根据您教我的事情分清主次的原则，我觉得先保证用户账号的创建，发送欢迎消息和给用户赠送积分这两件事件是不是可以晚点做？

**大师**：你的悟性很好，在设计一个产品的时候，先要保证主流程的通畅，其它次要的可以晚些做，晚些做，往往使用异步的方式来完成。

**小白**：大师，我知道了，创建用户是在主流程里面，使用数据库来insert账号，然后可以开启一个异步线程来执行insert message和update积分，如果异步线程执行失败了，把错误日志写到数据库中，后面来补偿执行。对了，不能每个用户一个异步线程，这样太耗费资源了，可以分别创建一个发送消息线程池，一个赠送积分线程池，当创建用户时，从这两个线程池中拿一个可以复用的线程来执行操作。

**大师**：你想的很周到，已经有很对人和你有同样的做法，你有没有更好的思路了呢？

**小白**：大师，能否提示一下思路？

**大师**：在设计系统的时候，有一个思想比较著名，它就是解耦。解耦的意思就是我把事情分成三个部分，不是希望一个人根据先后顺序去做，而是分给三个去做，提升工作效率。解耦的最常用的方式就是消息队列，这就是我们本节实战的主要目的：破鞭式。破鞭式的招式你先记下来，背熟悉后一一尝试使用。

序号 命令及描述

> 1 BLPOP key1 [key2 ] timeout
> 
> 移出并获取列表的第一个元素， 如果列表没有元素会阻塞列表直到等待超时或发现可弹出元素为止。
> 
> 2 BRPOP key1 [key2 ] timeout
> 
> 移出并获取列表的最后一个元素， 如果列表没有元素会阻塞列表直到等待超时或发现可弹出元素为止。
> 
> 3 BRPOPLPUSH source destination timeout
> 
> 从列表中弹出一个值，将弹出的元素插入到另外一个列表中并返回它； 如果列表没有元素会阻塞列表直到等待超时或发现可弹出元素为止。
> 
> 4 LINDEX key index
> 
> 通过索引获取列表中的元素
> 
> 5 LINSERT key BEFORE|AFTER pivot value
> 
> 在列表的元素前或者后插入元素
> 
> 6 LLEN key
> 
> 获取列表长度
> 
> 7 LPOP key
> 
> 移出并获取列表的第一个元素
> 
> 8 LPUSH key value1 [value2]
> 
> 将一个或多个值插入到列表头部
> 
> 9 LPUSHX key value
> 
> 将一个或多个值插入到已存在的列表头部
> 
> 10 LRANGE key start stop
> 
> 获取列表指定范围内的元素
> 
> 11 LREM key count value
> 
> 移除列表元素
> 
> 12 LSET key index value
> 
> 通过索引设置列表元素的值
> 
> 13 LTRIM key start stop
> 
> 对一个列表进行修剪(trim)，就是说，让列表只保留指定区间内的元素，不在指定区间之内的元素都将被删除。
> 
> 14 RPOP key
> 
> 移除并获取列表最后一个元素
> 
> 15 RPOPLPUSH source destination
> 
> 移除列表的最后一个元素，并将该元素添加到另一个列表并返回
> 
> 16 RPUSH key value1 [value2]
> 
> 在列表中添加一个或多个值
> 
> 17 RPUSHX key value
> 
> 为已存在的列表添加值

**大师**：好，你先熟悉这些招式，下午三点再将我们的实战项目的设计将给我听。

**小白**：谢大师指点！下午3点准时听候你的教导。

时间哒哒地流逝着，很快下午3点到了。小白也按时出现在大师面前。

**大师**：说说你怎么用redis做消息队列解决这个异步解耦的问题吧？

**小白**：大师，我回去后，练习了破索式的所有招式，发现有两种消息队列方式，又通过文档的查找，总共找到Redis 3中消息队列的方式，请大师帮我斧正一下：

1.LPUSH/RPOP非阻塞方式

Redis的队列list是有序的且可以重复的，作为消息队列使用时可使用rpush/lpush操作入队，使用lpop/rpop操作出队。当发布消息是执行lpush命令，将消息从列表左侧加入队列。消息接收方执行rpop命令从列表右侧弹出消息。

优点：实现简单

缺点：对于消费者而言存在一个问题，需要不停的调用rpop查看列表中是否有待处理的消息。每调用一次都会发起一次连接，势必造成不必要的资源浪费。如果使用休眠的方式让消费者线程间隔一段时间再消费，但这样做也有两个问题：

如果生产者速度大于消费者消费的速度，消息队列长度会一直增大，时间久了会占用大量内存空间。

如果休眠时间过长，就无法处理一些时效性的消息。如果休眠时间过短也会在连接上造成比较大的开销。

2.LPUSH/BRPOP阻塞方式

对于Redis来说提供了blpop/brpop阻塞读，阻塞读在队列没有数据时会立即进入休眠状态，一旦数据到来则立即被唤醒，消息的延迟几乎为零。需要注意的是如果线程一直阻塞在那里，连接就会被服务器主动断开来减少资源占用，这时blpop/brpop会抛出异常，所以编写消费段时需要注意异常的处理。

注意设置超时时间

```
BRPOP key [key ...] timeout
```

优点：数据可靠性提高了，只有在Redis宕机且数据没有持久化的情况下会丢失数据。

缺点：没有ack机制

3.发布订阅机制模型

Redis自带pub/sub机制即发布订阅模式，此模式中生产者producer和消费者consumer之间的关系是一对多的，也就是一条消息会被多个消费者所消费，当只有一个消费者时可视为一对一的消息队列。

优点：支持多个消费者

缺点：数据可靠性不太高，且没有ack机制

**大师**：你做的不错。常用的消息队列都做了总结。我们来看看有没有更好的实现方式呢？

比如Redis Stream，Redis Stream是最新(5.0以后版本)增加的数据结构，本质上是在Redis内核上（非Redis Module）实现的一个消息发布订阅功能组件。相比于现有的PUB/SUB、BLOCKED LIST，其虽然也可以在简单的场景下作为消息队列来使用，但是Redis Stream无疑要完善很多。Redis Stream提供了消息的持久化和主备复制功能、新的RadixTree数据结构来支持更高效的内存使用和消息读取、甚至是类似于Kafka的Consumer Group功能。

它的招式主要有：

> 命令 说明
> 
> XACK 结束 Pending
> 
> XADD 生成消息
> 
> XCLAIM 消息转移
> 
> XDEL 删除消息
> 
> XGROUP 消费组管理
> 
> XINFO 得到消费组信息
> 
> XLEN 消息队列长度
> 
> XPENDING Pending列表
> 
> XRANGE 获取消息队列中消息
> 
> XREAD 消费消息
> 
> XREADGROUP 分组消费消息
> 
> XREVRANGE 逆序获取消息队列中消息
> 
> XTRIM 消息队列容量

招式你先记住，回去以后勤加练习就行了。

注意，使用lpush/rpop，lpush/brpop，或者pub/sub时要有容错处理。

还有，要关注一下watch 这个招式，它可以标记所有指定的key 被监视起来，在事务中有条件的执行（乐观锁）。今天就到这里了！

**小白**：谢谢大师，恭送大师！

参考资料

【1】https://www.jianshu.com/p/02a923fea175
